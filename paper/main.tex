% Error fields, not error norms
% JCP Regular Article, REVTeX 4.2 (AIP).
%
% Main-text word count (Sections 1-9 plus the declarations and data-availability
% boilerplate, excluding abstract, references, figure captions, table contents
% and appendices): 9,078 words -- inside the 8,000-9,200 target. Counted with
% paper/wordcount.py, which strips the preamble, the abstract, all float
% environments, the appendices and the bibliography, counts a cross-reference as
% one word and a superscript citation numeral as none. Section-by-section, all
% within +/-20% of their targets: Introduction 770 (900), Theory 1126 (1300),
% Methods 1372 (1400), Results 2455 (2400), What did not work 1331 (1200),
% Relation to prior work 898 (900), Scope and limitations 512 (500), Conclusion
% 327 (350), Supplementary material 62 (60). Abstract 246 words.
%
% Every number in this manuscript is traceable to a file under results/ or to a
% deposited script that recomputes it from those files; see the traceability
% comments beside each table. Quantities added in revision are recomputed by
% scripts/compute_revision_statistics.py, which writes
% results/revision_statistics.json.
%
% Table ordering follows the audited evidence narrative. The retired legacy
% SPEC had imposed internally conflicting numbering rules.
%
\documentclass[aip,jcp,reprint,amsmath,amssymb,floatfix]{revtex4-2}

\usepackage{graphicx}
\usepackage{bm}
\usepackage{array}
\graphicspath{{../figures/}{./figures/}}

% AIP decimal section numbering: 1., 1.1., 1.1.1.
\renewcommand{\thesection}{\arabic{section}.}
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}.}
\renewcommand{\thesubsubsection}{\arabic{section}.\arabic{subsection}.\arabic{subsubsection}.}
% Figures arabic, tables Roman (AIP). REVTeX 4.2 defaults to this; set
% explicitly so a class-option change cannot silently break it.
\renewcommand{\thefigure}{\arabic{figure}}
\renewcommand{\thetable}{\Roman{table}}

\newcommand{\dU}{\ensuremath{\delta U}}
\newcommand{\avg}[1]{\ensuremath{\langle #1 \rangle}}
\newcommand{\avgz}[1]{\ensuremath{\langle #1 \rangle_{0}}}
\newcommand{\Cov}{\ensuremath{\mathrm{Cov}}}
\newcommand{\Var}{\ensuremath{\mathrm{Var}}}
\newcommand{\eVA}{\ensuremath{\mathrm{eV}/\text{\AA}}}

\begin{document}

\title{Error fields, not error norms: how the error in a fitted interatomic
potential reaches a physical observable}

\author{[AUTHOR NAME]}
\email{[EMAIL]}
\affiliation{[AFFILIATION]}

\begin{abstract}
% 246 words (counted by paper/wordcount.py).
Machine-learned interatomic potentials are selected on force RMSE and then used
to compute observables; we audit that inference. For a static observable $A$,
perturbation theory gives the leading shift as $-\beta\operatorname{Cov}_0(A,
\delta U)$. That is a projection; force RMSE is a gradient norm. On an analytic
reference where $\delta U$ is known
everywhere, legacy artifacts report four constructed error fields differing in
force RMSE by 0.52\% whose reported target shifts differ by 10.11 pairs. Their
paired uncertainty was not saved. This is a provisional observation in one
fixed Lennard--Jones cell,
pending a clean exp11 v3 replication, and not evidence about prevalence among fitted
potentials. Predictions have descriptive rms residuals of $1.01\sigma$ over
eight designed fields and $1.06\sigma$ over ten
fitted models, but shared reference trajectories make those rows correlated and
an independent end-to-end equivalence test remains underpowered; these are not
calibration passes. In a fixed, clustered zoo of 34 designed surrogates, force
RMSE ranks observable error across a 361-fold range (descriptive Spearman
$\rho=0.83$) but not inside an analyst-chosen factor-2.6 band ($\rho=0.34$),
where the response score gives $\rho=0.94$. Row-bootstrap intervals for this zoo
have no model-population estimand, so these numbers do not establish a selector.
One proposed heuristic fails: our smoothness-ratio metric carries no information
($\rho = -0.02$ [$-0.37$, 0.35]). A second, $w^{3/2}$ width scaling, was
retracted analytically; the four-point unswitched measurement is too uncertain
to test its exponent. Scope: one Lennard--Jones state point, one pair-radial
observable and designed fields.
\end{abstract}

\maketitle

%======================================================================
\section{Introduction}
\label{sec:intro}
%======================================================================

A machine-learned interatomic potential is fitted to energies and forces, its
root-mean-square force error on a held-out set is reported, the model runs
dynamics, a physical observable is extracted, and the
observable is believed. Every step is routine, and the chain
encodes an inference: small force error implies good physics. This paper
asks what connects the two ends, and finds it is not
what the reported metric measures.

The mismatch is structural. Writing the fitted
potential as $U = U_0 + \dU$, where $U_0$ is the reference and $\dU$ the
\emph{error field} --- a function on configuration space, not a number --- the
shift in a static ensemble average $\avg{A}$ is, at leading order, a linear
functional of $\dU$, while the squared force RMSE is a quadratic functional of
$\nabla\dU$. Relating those quantities requires ensemble geometry (for example
a Poincar\'e constant), and no task-agnostic ordering follows from the two
definitions alone. Section~\ref{sec:results} gives one constructive example.

Testing the claim requires knowing $\dU$, which in practice is exactly
what one does not have: the reference is density-functional theory, available on
a finite set of configurations. We therefore take an analytic classical
potential as ground truth. Lennard-Jones argon\cite{Jones1924, Rahman1964,
Verlet1967} is uninteresting as physics and exact as a reference, and that is
the point: because $U_0$ is known everywhere rather than
on a test set, $\dU$ is exactly computable at every sampled configuration,
reference observables converge to arbitrary precision, and --- decisively ---
error fields can be \emph{designed} to have chosen statistical properties. That
converts an observational claim into a constructive one: legacy artifacts report
fields separated by 0.52\% in force RMSE and 10.11 pairs in one target. Their
paired uncertainty is unavailable, and the result awaits clean exp11 v3 replication.

The empirical half of this story is known. Benchmarks that run
simulations rather than only scoring held-out
forces report that force error and simulation quality come
apart\cite{Fu2023, Stocker2022}; the recommendation to validate on
simulation observables is explicit in the methodological
literature\cite{Morrow2023}; and practitioners report specific failures
a force RMSE does not anticipate\cite{Kovacs2021, Deng2025, Pota2024}. Our
statistical machinery is equally standard: the identity below is
Zwanzig's free-energy-perturbation relation\cite{Zwanzig1954} expanded in
cumulants, and the same covariance appears as a parameter gradient in
differentiable trajectory reweighting\cite{Thaler2021} and as the vehicle for
propagating committee uncertainty to thermodynamic
averages\cite{Imbalzano2021}. We claim no novelty for it. What we claim is
narrower: the reading of
that identity as an indictment of the reported evaluation metric; the
analytic-oracle construction that makes $\dU$ exactly known; and the
constructive matched-force-RMSE falsification. The across-decades/within-band
decomposition is retained only as a descriptive observation on one fixed,
clustered zoo because its band was post hoc and has no model-population estimand.

\paragraph*{Contributions.}
\begin{enumerate}
\item A task-specific restatement and implementation of the standard
free-energy-perturbation result: the leading error in a static observable is a
covariance between that observable and the error field, with the cumulant
series made explicit (Sec.~\ref{sec:theory}). This identity is not claimed as
novel.
\item Descriptive root-mean-square prediction residuals of $1.01\sigma$ over
eight designed fields and $1.06\sigma$ over ten fitted models, accompanied by an
audit showing that shared reference samples invalidate an independent-residual
calibration reading. Corrected chain-level validation is not yet complete
(Secs.~\ref{sec:prediction} and \ref{sec:fitted}).
\item A provisional legacy counterexample set, pending clean exp11 v3 replication:
four fields differing in force RMSE by 0.52\%, whose reported target-observable
shifts run from $-0.22 \pm 0.64$ to
$+9.89 \pm 0.61$ pairs, ordered by $\rho(A,\dU)$
(Sec.~\ref{sec:counterexamples}).
\item A descriptive decomposition within one fixed designed zoo: force RMSE is
strong across a 361-fold range and unresolved within one post hoc narrow band.
The clustered construction and lack of a sampling population preclude a general
model-selection claim (Sec.~\ref{sec:regimes}).
\item Two failed predictions of our own and one analytically retracted but
experimentally unresolved scaling, reported as results rather than caveats
(Sec.~\ref{sec:failures}), with an unresolved 35\% discrepancy
between two ways of measuring the same shift.
\item A methodological result of independent interest: a reference ensemble that
was a slowly melting crystal, wrong in energy by 16--38\% and invisible to
every sampler diagnostic we had (Sec.~\ref{sec:equilibration}); the same
guard later excluded two fitted models whose apparent observable
shifts were twenty times any real effect here (Sec.~\ref{sec:fitted}).
\end{enumerate}

Section~\ref{sec:failures}, ``What did not work,'' is not an appendix and not a
limitations paragraph. Two predictions made in this study were contradicted by
its fixed panel: a smoothness-ratio replacement diagnostic and the prediction
that force RMSE would rank models weakly. A $w^{3/2}$ width scaling was retracted
on analytic grounds before measurement, but the compliant four-point subset is
too uncertain to test 1.5. Force RMSE instead ranks
surrogates \emph{well} across decades and poorly only inside a narrow band ---
and the corrected statement is more useful than the one we set out to
demonstrate.

%======================================================================
\section{Theory}
\label{sec:theory}
%======================================================================

\subsection{Setup}

Fix $N$ atoms in a volume $V$ at temperature $T$, $\beta = 1/k_BT$, and let
$x \in \mathbb{R}^{3N}$ be a configuration. The reference is $U_0(x)$
and the surrogate is
\begin{equation}
U(x) = U_0(x) + \dU(x),
\end{equation}
so the error field $\dU$ is a function on configuration space, and the error in
the forces is its gradient, $\delta F = -\nabla \dU$. The scalar summary in
universal use is
\begin{equation}
\mathrm{RMSE}_F^2 = \frac{1}{3N}\,\big\langle \|\nabla \dU\|^2 \big\rangle_{\rm test}.
\label{eq:frmse}
\end{equation}
Canonical averages in the two ensembles are $\avgz{\cdot}$ and $\avg{\cdot}_U$,
and the quantity of interest is the observable error
$\Delta\avg{A} \equiv \avg{A}_U - \avgz{A}$.

\subsection{The exact relation}

Multiplying and dividing by $e^{-\beta U_0}$ gives, exactly,
\begin{equation}
\avg{A}_U = \frac{\avgz{A\,e^{-\beta \dU}}}{\avgz{e^{-\beta \dU}}} .
\label{eq:fep}
\end{equation}
This is the free-energy-perturbation identity\cite{Zwanzig1954}, exact for any
$\dU$, and it already carries the argument of this paper: the surrogate ensemble
is determined by the joint distribution of $(A, \dU)$ under the reference
ensemble and by nothing else, so two surrogates with identical
$(A,\dU)$ statistics predict identical physics for $A$ whatever their
architectures or force errors. Equation~\eqref{eq:fep} is
numerically treacherous, the exponential average being dominated by rare
configurations where $\dU$ is very negative\cite{Bennett1976, ShirtsChodera2008,
FrenkelSmit2002}, so we also need its expansion.

\subsection{Cumulant expansion}
\label{sec:cumulant}

Expanding numerator and denominator of Eq.~\eqref{eq:fep} in powers of
$\beta\dU$ and collecting orders gives the central result.

\paragraph*{Result 1 (response expansion).}
\begin{equation}
\Delta\avg{A} = \sum_{n \ge 1} \frac{(-\beta)^n}{n!}\,
\kappa_0\!\left(A; \underbrace{\dU,\dots,\dU}_{n}\right),
\label{eq:cumulants}
\end{equation}
with $\kappa_0$ the joint cumulant under the reference ensemble. To
second order, with $\tilde A = A - \avgz{A}$ and
$\widetilde{\dU} = \dU - \avgz{\dU}$,
\begin{equation}
\Delta\avg{A} = -\beta\,\Cov_0(A,\dU)
 + \frac{\beta^2}{2}\big\langle \tilde A\,\widetilde{\dU}^2 \big\rangle_0 - \dots
\end{equation}

\paragraph*{Result 1a (linear response).}
\begin{equation}
\Delta\avg{A} = -\beta\,\Cov_0(A, \dU) + O(\dU^2).
\label{eq:linear}
\end{equation}

Three consequences carry the paper.

\emph{(i) It is a covariance, not a force norm.} $\Cov_0(A,\dU)$ can vanish
for an error field with large variance. Conversely, a small \emph{force} norm
does not by itself bound $\sigma_0(\dU)$ without an ensemble-dependent
Poincar\'e-type constant and a fixed energy gauge. Factoring the covariance into
scales and a correlation is a definition,
not an estimate,
\begin{equation}
\Delta\avg{A} \;=\; -\beta\,\sigma_0(A)\,\sigma_0(\dU)\,
\rho_0(A,\dU) \;+\; O(\dU^2),
\label{eq:cs}
\end{equation}
with $\rho_0$ the Pearson correlation under the reference ensemble.
Force RMSE reports none of these three factors, especially the task-specific
correlation, which may range over $[-1,1]$.

Bounding the first-order term rather than factoring it gives the
Cauchy--Schwarz inequality,
\begin{equation}
\big|\Delta^{(1)}\avg{A}\big| \;\le\;
\beta\,\sigma_0(A)\,\sigma_0(\dU),
\label{eq:cs2}
\end{equation}
obtained by setting $|\rho_0| \le 1$. It bounds response through
\emph{energy-field variance}, not held-out force RMSE, and is tight only for a
perfectly aligned error field.

\emph{(ii) It needs only reference-ensemble samples.} Every expectation in
Eq.~\eqref{eq:linear} is under $\avgz{\cdot}$, so given a stored reference
trajectory and the ability to evaluate $U$ and $U_0$ on its frames,
$\Delta\avg{A}$ can be estimated without simulating the surrogate: one energy
evaluation per stored frame per model. This computational route is standard in
single-trajectory reweighting and committee uncertainty propagation
\cite{Imbalzano2021}, so it is not a contribution claimed here. In the oracle
study it also makes the mechanism
falsifiable, since prediction and measurement are separate experiments.

\emph{(iii) The second-order term is a correction, not a gate.} The $n = 2$
term is estimable from the same samples and can be used two ways, which came out
differently.

\emph{Added to the prediction, it may correct truncation.} In the legacy exp09
panel it reduces some descriptive residual summaries, but that panel omits the
correction's uncertainty and shared-reference covariance and reuses common
random streams. It therefore does not establish bias removal or calibrated
coverage (Sec.~\ref{sec:calibration}).

\emph{Used as a threshold, it does not discriminate in the available audit.}
The obvious self-diagnostic --- pass the prediction where the ratio of second to
first order is small --- contains explicit false passes. A pooled audit gives a
descriptive AUC of 0.556, but its 89 rows are clustered and cannot be treated as
independent cases (Sec.~\ref{sec:warninglight}). \textbf{The gate claim is
withdrawn.} We compute the term descriptively and certify nothing with it.

\subsection{Vector observables, and the contrast with free energy}

The radial distribution function is a vector of bin
occupancies $A_k$, and Eq.~\eqref{eq:linear} applies componentwise, giving a
predicted difference curve $\Delta\avg{A_k} = -\beta\,\Cov_0(A_k, \dU)$. The
natural summary of ``how wrong is the structure'' is a norm of that vector,
taken \emph{after} the projection --- the opposite
order of operations from force RMSE, where the norm comes first and no
projection is performed at all.

For contrast, the free energy obeys\cite{Zwanzig1954}
\begin{equation}
\Delta F = -\frac{1}{\beta}\ln\avgz{e^{-\beta\dU}}
= \avgz{\dU} - \frac{\beta}{2}\Var_0(\dU) + \dots,
\end{equation}
so free-energy error depends first on the mean error field while observable
errors depend on covariances. A task-independent scalar cannot preserve every
ordering; after observables and tolerances $\tau_j$ are fixed, however,
$\max_j|\Delta\avg{A_j}|/\tau_j$ is a valid task-specific scalar.

\subsection{Why this particular norm}
\label{sec:frequency}

The following frequency-space argument is an untested heuristic, not a result
about fitted-model prevalence. Result 1a says a norm alone does not determine a
task-specific projection; under additional smoothness assumptions one can also
see why a gradient norm may weight constructed directions differently.
Expand $\dU = \sum_k c_k \varphi_k$ in an orthonormal basis labelled by
spatial frequency; then
\begin{align}
\Cov_0(A,\dU) &= \sum_k c_k \,\Cov_0(A, \varphi_k), \label{eq:modecov}\\
\big\langle\|\nabla\dU\|^2\big\rangle &\sim \sum_k k^2 |c_k|^2. \label{eq:modeforce}
\end{align}
the gradient in Eq.~\eqref{eq:modeforce} contributes a factor $k^2$, so force
error weights the error spectrum by the square of its spatial frequency. The
covariance applies no such weight, and because
for observables with sufficiently regular mode couplings, $\Cov_0(A,
\varphi_k)$ may decay with $k$. In that restricted setting the functionals can
weight the same spectrum in opposite directions. A possible \emph{false alarm}
is a high-frequency interpolation wiggle: it can be amplified by $k^2$ in force
error while coupling weakly to a declared smooth observable. A possible
\emph{false confidence} is a smooth systematic error that is weak in a gradient
norm but couples to that observable. The current experiments do not establish
how often either direction occurs in regularised or fitted MLIPs; that is Gate B.

\subsection{A heuristic, retracted in advance}
\label{sec:retraction}

It is tempting to make Sec.~\ref{sec:frequency} quantitative. Perturb a pair
potential by a Gaussian shell bump of
amplitude $a$, centre $r_0$ and width $w$, $\delta u(r) = a\exp[-(r-r_0)^2 /
2w^2]$: its gradient scales as $a/w$ and the pair fraction inside the shell
as $w$, giving $\mathrm{RMSE}_F \sim a/\sqrt{w}$, while its coupling to $g(r)$
is an integral over the shell, $|\Cov_0(g,\dU)| \sim a\,w$. At fixed force error
the ratio would grow as
\begin{equation}
\big|\Delta\avg{g}\big| \big/ \mathrm{RMSE}_F \;\sim\; w^{3/2},
\label{eq:w32}
\end{equation}
which is unbounded: two error fields with identical force RMSE could differ in
observable error by any factor one likes.

We retract Eq.~\eqref{eq:w32} as a prediction, on analytic grounds and in
advance of the measurement. Counting pairs in the shell as $\propto w$ holds
only while $w$ is small compared with the scale on which $g(r)$ varies, a few
tenths of an \AA ngstr\"om near the first peak of liquid argon, so the
estimate is marginal already at $w = 0.3$~\AA; and treating the observable as
integrating the whole bump fails once $w$ exceeds the bin width, because the
coupling saturates and beyond saturation the bump pushes pairs into
a bin from one side while pushing them out on the other. Neither objection needs
a measurement. What survives, and what we test, is that force error and
observable error depend differently on the \emph{shape} of the error field, so
their ratio is not constant --- Sec.~\ref{sec:width} measures it
varying by 4.66. Section~\ref{sec:w32} explains why the four-point unswitched
subset is too uncertain to test the exponent and why the old six-point slope is
confounded by switching-region truncation.

\subsection{What the theory does not cover}
\label{sec:dynamics}

Everything above concerns static averages, not the perturbed propagator for
transport coefficients. Force error may matter more directly for dynamics
\cite{Wu2024}, but the planned static-versus-dynamical comparison was not run;
nothing here tests that conjecture.

%======================================================================
\section{Methods}
\label{sec:methods}
%======================================================================

\subsection{System}

The reference is Lennard-Jones argon\cite{Jones1924}, $N = 108$ atoms in
a cubic periodic cell at $\rho = 0.0200$~\AA$^{-3}$ and $T = 120$~K; in reduced
units $\rho^* = \rho\sigma^3 = 0.790$, $T^* = k_BT/\epsilon = 1.004$, below the
freezing density\cite{Verlet1967,
HansenMcDonald2013}. Parameters are $\epsilon = 0.0103$~eV,
$\sigma = 3.405$~\AA, cutoff $7.0$~\AA, in the \emph{shifted-force} form. That
mode is not cosmetic: Table~\ref{tab:cutoff}
(Appendix~\ref{app:validation}) gives the four modes' discontinuities at
$r_c \mp 10^{-7}$~\AA, and a merely shifted potential leaves a force jump of
$1.8\times10^{-4}~\eVA$ --- an impulse at every cutoff crossing, and
indistinguishable in this study from the effect under investigation.

\subsection{Sampling}

Static observables are sampled with Hamiltonian Monte
Carlo\cite{Duane1987, Neal2011}, not molecular dynamics: each proposal draws
momenta from Maxwell--Boltzmann, integrates a short velocity
Verlet trajectory\cite{Swope1982}, and accepts on the change in the total
Hamiltonian. Because velocity Verlet is symplectic and time-reversible the
proposal is symmetric, so the stationary distribution is exactly $e^{-\beta U}$
\emph{regardless of integration error}, which appears only as reduced
acceptance. That forecloses the objection
that the thermostat is the systematic error: a slightly non-canonical thermostat
would produce exactly the difference this study attributes to model error.
Production settings are 8 leapfrog steps per proposal, step size adapted in
burn-in to acceptance 0.75, giving $\approx 30$~fs; an
independent single-particle Metropolis sampler\cite{Metropolis1953} sharing no
code beyond the potential is a cross-check. The sampler was validated against
exact equipartition on a 32-atom Einstein
crystal at 100~K: $\avg{U} = 0.4146 \pm 0.0020$~eV against the exact
$0.41363$~eV ($0.5\sigma$), and $\Var(U) = 0.003459$~eV$^2$ against
$0.003564$~eV$^2$ (Table~\ref{tab:validation}); both moments
matter, since a wrong temperature scale reproduces the mean and fails the
variance.

\subsection{Equilibration, and the failure that made it necessary}
\label{sec:equilibration}

The liquid is built by scaling an fcc lattice to liquid density. That
configuration is a crystal, and sampling it directly produces a slowly melting
crystal: on the original protocol the global Steinhardt bond-order
parameter\cite{Steinhardt1983, LechnerDellago2008} $Q_6$ fell monotonically from
0.5745, the perfect fcc value, to 0.43 over 600 proposals and was still falling,
while the potential energy climbed and was still climbing. The
equilibrated liquid sits at $-0.0311$~eV/atom; those runs sampled $-0.036$
to $-0.043$, so the reference ensemble was wrong in energy by 16 to
38\% (our notes round that to ``30--40\%'').

No sampler diagnostic caught it: acceptance rate and integration error were
healthy throughout, because they measure whether the chain is
\emph{simulated} correctly, not whether it has reached stationarity --- and
only the second bears on an ensemble average.

The protocol now used melts at $5\times$ the target temperature for a few
hundred proposals, anneals, and refuses to return a
configuration with $Q_6 > 0.20$ (a 108-atom liquid gives
$Q_6 \approx 0.08$, perfect fcc 0.5745). Afterwards
$Q_6 = 0.074$ and $U/N = -0.0315$~eV, with no drift
between halves of the production run. Every production trajectory is
split in half and both energy and order parameter compared between
halves; a drift exceeding three combined standard errors \emph{raises} rather
than warns, since a failure mode with no outward sign will be ignored if it
only warns. The same check runs on each fitted surrogate's
trajectory, which has its own equilibrium and need not have reached
it; Sec.~\ref{sec:fitted} reports the one occasion it fired. Quick-run
mode never shortens equilibration.

\subsection{Observables}

The measured quantities are pair counts in radial bins,
$A_k(x) = |\{(i,j) : r_k \le r_{ij} < r_{k+1}\}|$, on eight bins spanning 3.0 to
7.0~\AA{} (centres 3.25, 3.75, \dots, 6.75~\AA). They differ from $g(r)$ in that
bin only by a constant at fixed $N$ and $V$\cite{AllenTildesley2017}; the
analysis uses counts because response theory applies to a plain function of
configuration. The designed-perturbation arm
of Sec.~\ref{sec:counterexamples} additionally targets a \emph{scalar}, the pair
count between 3.4 and 3.9~\AA{} --- deliberately
offset from the eight-bin grid, so a shift in it cannot be read off the
difference curve --- making the covariance a $K$-vector rather
than a $K\times J$ matrix. That is also a
limitation, restated in Sec.~\ref{sec:scope}: the headline demonstration
concerns one bin, and Sec.~\ref{sec:orthogonality} shows it does
not transfer to the others.

\subsection{Designed error fields}
\label{sec:designed}

Error fields are full potentials with analytic forces and virial, so a surrogate
is ordinary addition; all derivatives agree with
finite differences to $\sim 10^{-10}$ on cubic and sheared triclinic cells.
The constructions expand a pair error field in fourteen Gaussian
shell bumps, making the covariance with an observable \emph{linear} in the
coefficients, so choosing coefficients in its null space, or
parallel to it, is linear algebra rather than optimisation. That gives
\emph{null} fields, whose first-order predicted effect on
the target is zero by construction; \emph{aligned} fields, of maximal effect
per unit force error; and \emph{random} controls --- all scaled to the same
force RMSE, so the number a practitioner would report
is held fixed. Two null constructions appear below: \emph{scalar-null}, orthogonal to the
target bin, and \emph{curve-null}, orthogonal to all eight at once.

Out-of-sample discipline is essential, because a null field is orthogonal to the
observable \emph{on the frames it was built from} by definition. The reference
trajectory is split: fields are constructed on one half and every
reported number --- force RMSE, predicted shift, covariance, measured shift ---
computed on the other. How many construction frames this needs is itself
measured (Sec.~\ref{sec:prediction}): with a bad covariance estimate,
the null space found is that of the noise.

\subsection{Fitted models}
\label{sec:zoo}

Ten models were fitted to 400 configurations, training and test
data from separate Markov chains: two pair splines
(\texttt{spline\_k8}, \texttt{spline\_k24}); two linear
atomic-cluster-expansion-style bases\cite{Drautz2019, Bartok2013}
(\texttt{linear\_l2}, \texttt{linear\_l4}); Behler--Parrinello
networks\cite{Behler2007, Behler2011} at two widths and two initialisation seeds
each (\texttt{bpnn\_16x2}, \texttt{bpnn\_64x2}); and an $E(3)$-equivariant
message-passing network\cite{Batzner2022, Gilmer2017} at two seeds
(\texttt{egnn\_c8}). These are deliberately small surrogates of a
classical potential, not the published potentials\cite{Bartok2010,
Batatia2022, Schutt2018, Thompson2015} whose evaluation motivates the work;
those architectures are cited as background only. A
Gaussian-approximation-potential-style kernel
model\cite{Bartok2010} exists in the codebase but is not in the reported zoo.
Two further arms at smaller budgets appear in
Secs.~\ref{sec:fitted} and \ref{sec:breakdown}. The four
Behler--Parrinello records carry \texttt{fit\_converged = false} --- their
optimiser reached its iteration limit, not its tolerance --- and we
report them anyway, since their force errors are the zoo's smallest and
dropping them would be a post hoc filter.

\subsection{Uncertainty estimation}
\label{sec:uncertainty}

Every uncertainty here is a \textbf{standard error} from correlated-sample
statistics: $\pm$ always denotes one, never a standard deviation,
and $1/\sqrt{M}$ is never used, since treating correlated frames as
independent understates the error by $\sim\sqrt{2\tau_{\rm int}}$.
Means use Flyvbjerg--Petersen blocking\cite{Flyvbjerg1989}; covariances,
correlations and fitted slopes use a moving-block
bootstrap\cite{Kunsch1989, PolitisRomano1994}
at four integrated autocorrelation times of the
\emph{product} series $(A_m - \bar A)(\dU_m - \overline{\dU})$ that carries the
error, with a jackknife cross-check
(Appendix~\ref{app:statistics}). Deposited exp05 intervals use 2000 resamples;
those computed here by
\texttt{scripts/compute\_band\_correlations.py} and
\texttt{scripts/compute\_revision\_statistics.py} use seed 20240517, 2000
resamples for correlations and 20{,}000 for the width exponent.

Every correlation is reported as $\rho = \text{value}\;[\text{low},
\text{high}]$ with its $n$; significance as a $\sigma$-distance,
$|\text{value}|/\text{error}$, to one decimal place, with no $p$-values;
uncertainties to two significant figures and central values to the same decimal
place, the one exception being exactness checks
(Appendix~\ref{app:validation}). The deposited records hold unrounded values.
Prediction residuals in $\sigma$ are measured minus predicted, over
the standard error of the \emph{measurement}; within an arm every surrogate
is measured against one reference-trajectory mean, so those residuals are
not independent, and Sec.~\ref{sec:prediction} works through the consequence.

Two noise conventions exist, and we use one: the zoo's observable error is a
norm, inflated by noise as $\mathbb{E}|v_{\rm meas}|^2 =
|v_{\rm true}|^2 + \mathbb{E}|v_{\rm noise}|^2$, so all correlations here
use $|v_{\rm true}| = (\,|v_{\rm meas}|^2 -
\mathbb{E}|v_{\rm noise}|^2)^{1/2}$, floored at zero --- necessary at the
low-force-error end, where the raw norm is almost entirely noise. Raw-series
values appear beside the noise-subtracted ones in Table~\ref{tab:regimes}.

\subsection{Reproducibility and compute}

Legacy manifests record a completion-time commit, dirty flag, configuration,
timings, versions and seed. Across nine runs they name eight commits and sum to
31{,}339~s (8.7~h) on a four-core x86-64 Linux machine; exp11 is largest at
5{,}304~s. All nine report a dirty tree. For exp09 and exp11 the listed dirt is
limited to their own generated outputs, but even there the finish snapshot does
not prove which code was loaded at launch; earlier runs did not retain their
diffs. Thus the numerical source cannot be established from a named revision.
Corrected manifests take launch and finish snapshots and fail on intervening
source changes, but no corrected production run yet supports this manuscript.

%======================================================================
\section{Results}
\label{sec:results}
%======================================================================

\subsection{Four error fields within 0.52\% in force RMSE}
\label{sec:counterexamples}

Four error fields were built against one target observable, the pair count
between 3.4 and 3.9~\AA, each scaled to a prescribed force RMSE at two
levels, nominally $1.0\times10^{-3}$ and $4.0\times10^{-3}~\eVA$. Measured out
of sample --- on the half of the trajectory not used for
construction --- they agree in force RMSE to within 0.52\% at
each level: $0.99535$ to $1.00054 \times 10^{-3}~\eVA$, and
$3.9814$ to $4.0022\times10^{-3}~\eVA$ (Table~\ref{tab:counterexamples},
Fig.~\ref{fig:mechanism}(a)).

At the upper level the measured shifts run from
$-0.22 \pm 0.64$ pairs for the scalar-null field ($0.3\sigma$) to
$+9.89 \pm 0.61$ for the aligned field ($16.2\sigma$), the
random controls between at $-1.48 \pm 0.58$ ($2.6\sigma$)
and $+5.32 \pm 0.59$ ($9.0\sigma$); at the lower level the same fields
give $-0.55 \pm 0.58$ ($0.9\sigma$), $-1.45 \pm 0.58$ ($2.5\sigma$),
$+0.84 \pm 0.59$ ($1.4\sigma$) and $+1.83 \pm 0.58$ ($3.2\sigma$) --- not the
upper values divided by four. The \emph{predictions} do scale with the force
level (2.55 against 10.21 pairs, aligned), being linear in $\dU$;
the lower-level measurements are simply noisier against their own signal.

The sharpest form of the comparison is the \emph{difference} between the
extreme fields, because every field here is measured against the same
reference-trajectory mean (Sec.~\ref{sec:uncertainty}), which cancels
identically in a difference. At the upper level the aligned field sits
$10.11$ pairs above the scalar-null. The legacy table's naive independent-error
quadrature reports $11.4\sigma$, but the paired-stream covariance was not saved
and this is not a valid confirmatory interval.

Two conclusions follow. First, under a predeclared practical band at least as
wide as the observed 0.52\% force-RMSE separation, an RMSE-only selector would
treat these fields as equivalent while their target shifts differ by more than
ten pairs. This is a resolution statement; the non-identical RMSE values do not
make exact indistinguishability a mathematical fact. Second, what does distinguish
them is $\rho(A,\dU)$: ordering the fields by it ---
$+0.110$, $+0.009$, $-0.430$, $-0.582$ for random seed 1, null, random seed 0
and aligned --- orders the measured shifts monotonically and near-linearly at
both levels [Fig.~\ref{fig:mechanism}(a)], as
$\Delta\avg{A} = -\beta\,\sigma_0(A)\,\sigma_0(\dU)\,\rho_0$ requires when the
first two factors are nearly fixed. This is a provisional fixed-cell legacy
observation, not a frequency claim, until the corrected exp11 v3 replication runs.

\subsection{Prediction agreement is descriptive, not calibrated}
\label{sec:prediction}

The predicted column of Table~\ref{tab:counterexamples} is
$-\beta\,\Cov_0(A,\dU)$ on one reference trajectory; the measured column is
explicit sampling of each perturbed potential. The eight standardized residuals
have rms $1.01\sigma$, mean $-0.86\sigma$ and scatter $0.57\sigma$. Those eight
rows are not independent: they share a reference mean, construction trajectory,
field family and parts of the random design. Consequently the sign-test and
one-sample $t$ statistics reported in an earlier draft have no valid sampling
unit and are withdrawn. The $1.01\sigma$ is a fixed-arm description only.

The first attempted replication (legacy exp09) does not settle calibration. It
crossed eight reference chains with one fixed eight-field panel and re-used each
field's four direct-chain means across all rows. Worse, all fields re-used the
same four direct random streams, the direct stationarity guard was disabled,
prediction uncertainty and its covariance with the shared reference mean were
omitted, and only aggregate records were deposited. The apparent 64 cells are
therefore not 64 independent measurements. A deleted smoke run was also
misreported as using 1500 frames per chain although quick mode scaled that
default to about 75; every number from that smoke run is withdrawn.

We have replaced that pipeline with an exp09 v3 design that freezes launch provenance,
uses field-specific chains and stationarity checks, stores per-frame evidence,
and jointly resamples independent reference and direct-chain units while
recomputing the reference mean and first- and second-order terms. At the time of
this manuscript correction, no production v3 result exists. Thus neither ``no
bias'' nor ``the error bar, not the estimator'' is established; calibration and
coverage remain open experimental questions.

The second-order term is likewise descriptive. In the original null-field arm
it is unresolved from zero and sometimes worsens agreement. The repository-wide
threshold audit contains correlated cases and so cannot supply binomial error
rates, but it does contain explicit false passes. We therefore report the term
without using it to certify a prediction.

One narrower construction-budget result remains useful. Null fields built on
250, 500, 1000 and 2000 frames and evaluated on disjoint frames have predicted
target shifts $0.041\pm0.067$, $0.142\pm0.071$, $0.037\pm0.077$ and
$0.028\pm0.078$ pairs, respectively, against an aligned prediction of
$10.21$ pairs. These are four nested prefixes of one construction trajectory,
not four independent replications; they show out-of-sample suppression in that
fixed cell and nothing more.

\subsection{Orthogonality is specific to the observable it was built for}
\label{sec:orthogonality}

The scalar-null field is orthogonal to one observable and one only
(Fig.~\ref{fig:counterexamples}). At $4\times10^{-3}~\eVA$ it leaves its target
bin alone at $-0.22 \pm 0.64$ pairs --- not one of the eight bins
plotted; the value is from Table~\ref{tab:counterexamples} --- while its
deposited difference curve has sizeable non-target excursions. An earlier draft
reported $11.36 \pm 0.95$ pairs by taking the largest of eight noisy bins; that
post-selection maximum and its unadjusted pointwise standard error are
withdrawn.

Orthogonality is therefore specific to the \emph{set} of observables a field
was built against --- and enlarging the set works. The curve-null field of
Sec.~\ref{sec:regimes}, built at the same $4\times10^{-3}~\eVA$ against all
eight bins at once, has a difference curve whose largest component is
$1.39$ pairs and whose norm, $2.65$ pairs against a noise level of $2.43$, is
the smallest in the zoo and not resolved from zero. Nulling one
scalar silences one bin; nulling the vector silences the curve --- which
corrects a stronger claim we might have made: harmlessness in general is not
impossible, only relative to a stated set of questions.

This limits the claim as much as it supports it: what
Sec.~\ref{sec:counterexamples} shows is force RMSE failing to predict
a \emph{particular} observable, not that any model is globally harmless. No
\emph{task-independent} scalar has been shown here to order models consistently
across observables. The present single-bin construction shows poor practical
resolution of force RMSE for that task under a tolerance of at least 0.52\%; it
is not a formal statistical-sufficiency result and does not prove that every
possible scalar loss must fail. A vector of response scores can be reduced to a
scalar only after task tolerances or utilities are stated. Both constructions use
the reference-ensemble covariance, which a practitioner lacks: they are an
instrument for separating an error's size from its shape, not a recipe.

\subsection{Across decades and within a band}
\label{sec:regimes}

The registered P1 expected weak rank correlation between force RMSE and
downstream error across an architecture-diverse zoo. The available panel instead
contains 34 designed surrogates spanning 361-fold in force RMSE. Across that
panel force RMSE correlates strongly with the eight-bin error norm,
$\rho=0.83$ [0.67, 0.93], $n=34$ (Table~\ref{tab:regimes}); thus P1 is
contradicted for this panel, but its named fitted-model population was not tested.

Inside an analyst-chosen, post hoc factor-2.59 force-error band, force RMSE gives
$\rho=0.34$ [$-0.26$, 0.77], whereas the response prediction gives
$\rho=0.94$ [0.76, 1.00], $n=15$. These are fixed-panel row-resampling
sensitivities, not population confidence intervals: members share field
families, reference data and random streams. The floor-sensitive
noise-subtracted spread is $30\times$, but the raw spread is $12\times$ and the
spread after removing the two designed extremes is $6.5\times$; six members
also tie at the same force RMSE. Subset checks are reported in
Table~\ref{tab:regimes}, not promoted to independent replications.

The warranted conclusion is therefore narrow: force RMSE is unresolved as an
ordering statistic inside this fixed designed band, while the response score is
not; neither fitted-model prevalence nor selection utility follows. Published
benchmarks likewise report average utility plus individual low-error failures
\cite{Gawkowski2026}, but do not validate this panel's breakpoint.

Two descriptive secondary results are retained. The proposed smoothness ratio
gives $\rho=-0.02$ [$-0.37$, 0.35], $n=34$, and is unsupported; per-atom energy
spread gives $\rho=0.92$ [0.82, 0.97], but its apparent advantage was not
predicted or independently tested. Unstable top-$k$ overlaps are not evidence.

\subsection{Models that were actually fitted}
\label{sec:fitted}

The designed fields test the mechanism; fitted models test whether it
survives contact with objects produced by fitting. Table~\ref{tab:zoo} lists
the ten models of Sec.~\ref{sec:zoo} with their force
RMSE and their predicted and measured shifts.

The response formula can be evaluated on them: the descriptive root-mean-square
residual across the ten is $1.06\sigma$, against $1.01\sigma$ on the designed
fields. The unvalidated linearity screen passes 8 of 10; this carries no
validity verdict.

\textbf{This is not an external-validity check, and an earlier draft of this
paper called it one.} The ten models share the system, the observable, the
training pool, the reference chain and the code path with the arm they were
supposed to check, and each network family has two initialisation seeds. At
most, the arm shows that the response calculation can be run on
training-induced error fields --- a feasibility result. Because most direct
truths are at the noise floor, it does not establish quantitative prediction
accuracy. The caution of
Sec.~\ref{sec:prediction} applies again: seven of ten residuals are negative,
their descriptive mean is $-0.49\sigma$ and scatter $1.00\sigma$, and this arm
too uses one shared reference mean. An ordinary one-sample test over the ten
rows would therefore use the wrong sampling unit and is not reported.

A third arm belongs on the record and was omitted from earlier drafts.
Ten models were also fitted to \emph{40} configurations; eight
gave a prediction residual of $1.00\sigma$ ($n = 8$), decomposing as above.
The other two matter more: both
\texttt{egnn\_c8} seeds gave apparent
shifts of $-95.5 \pm 1.7$ and $-112.8 \pm 2.8$ pairs, twenty times
any real effect here, and the stationarity check of
Sec.~\ref{sec:equilibration} found both chains unequilibrated
and excluded them. \textbf{Excluding models from an arm on a diagnostic is a
disclosure obligation, discharged here:} without that check the largest
numbers in this paper would have been transients.

Without softening: \textbf{the rank correlations from this arm
are not reportable.} Force RMSE spans only $4.0\times10^{-4}$ to
$6.1\times10^{-3}~\eVA$ and buys almost no measurable
difference, observable error spanning 0.049 to 4.10 pairs, mostly at the
noise floor: at this budget on this reference,
nearly every architecture is good enough that its observable error is
indistinguishable from zero. Every interval spans zero ---
force RMSE $\rho = 0.43$ [$-0.35$, 0.96], force MAE $\rho = 0.35$ [$-0.49$,
0.92], energy RMSE $\rho = 0.05$ [$-0.77$, 0.76], smoothness ratio
$\rho = -0.53$ [$-0.86$, 0.06], response prediction $\rho = 0.18$ [$-0.70$,
0.83], all $n = 10$. Nothing about ranking can be concluded from this arm, and
nothing is; the $1.06\sigma$ is a fixed-arm descriptive residual.

One pair is worth stating on its own, weakness attached.
\texttt{egnn\_c8\_s0} and \texttt{egnn\_c8\_s1} share architecture,
training data and hyperparameters, differing only in seed.
Their force RMSE differs by 35\%, $0.0045$ against
$0.0061~\eVA$; their measured target-bin errors are $-0.15 \pm 0.57$ pairs
($0.3\sigma$) and $-4.10 \pm 0.69$ ($6.0\sigma$), a factor of 27, both
predicted from the reference trajectory at $-0.98$ and $-3.03$ pairs. Anyone
selecting between them on force
RMSE would see a 35\% difference and reasonably call them equivalent.
\textbf{This is $n = 2$:} the paper's most quotable observation and
its weakest evidence. Seed
variance is a known and under-reported source of spread in machine-learning
benchmarks\cite{Bouthillier2021, Pineau2021, Kapoor2024}; repeating it
at $n \ge 8$ seeds is the highest-value small follow-up.

Both EGNN records fail the legacy linearity screen, with second-order ratios of
0.41 and 0.14 against a median of 0.015 across the other eight. The largest
$|$residual$|$, however, is \texttt{linear\_l2} at $-1.60\sigma$ and it passes
the screen. This is another false pass, not partial validation.

\subsection{Damage varies 4.66-fold at fixed force error}
\label{sec:width}

The final designed experiment holds force RMSE constant and varies only the
width $w$ of the error field in $r$. Six Gaussian shell fields at
$r_0 = 4.2$~\AA{} and $w$ from 0.10 to 1.00~\AA{} were amplitude-scaled to
$2.0\times10^{-3}~\eVA$, with a residual force-RMSE spread of
$3.3\times10^{-15}~\eVA$ --- exactly fixed.
The measured norm of the difference curve spans a factor of 4.66, from
$2.1 \pm 2.1$ pairs at $w = 0.10$~\AA{} to $9.9 \pm 2.5$ at
$w = 0.65$~\AA{} [Fig.~\ref{fig:mechanism}(b)], and the first-order
prediction tracks the rise, from 3.22 to 9.68 pairs, with
much smaller uncertainty. The direct measurements at small $w$ carry
error bars comparable to their own values, so the ordering is
carried mainly by the prediction: honestly stated, the observed range is
$4.7\times$ with the lowest point consistent with zero. The sweep is also not monotone --- the largest width falls
back to $8.4$ pairs, the saturation Sec.~\ref{sec:retraction}
anticipated --- and Sec.~\ref{sec:w32} takes up what that does to a fitted
exponent.

\subsection{Why the first calibration replication is superseded}
\label{sec:calibration}

The legacy exp09 table contains useful exploratory point estimates but cannot
support a calibration conclusion. Its design is crossed, not 64-fold
independent: eight reference chains are combined with the same fixed field panel
and the same four direct means per field. Direct random streams are shared across
fields, the per-field chain errors are strongly heteroskedastic, and the
standardized residual omitted prediction uncertainty and all shared-reference
covariances. A pooled standard error and an ordinary cell count cannot repair
those dependencies. Because per-frame and per-chain source values were not
deposited, a valid joint reanalysis is impossible.

Accordingly, the legacy row/column variance ratios, tests of a grand mean, and
the statement that the second-order term explains the field structure are all
withdrawn. The result is retained only as motivation for the corrected exp09 v3
design described in Sec.~\ref{sec:prediction}. Until that production run is
completed from a frozen source snapshot, the estimator's bias, coverage and
field-specific residual structure are unanswered.

\subsection{Fixed-cell construction-seed sensitivity}
\label{sec:replication}

Sec.~\ref{sec:counterexamples} establishes the aligned--null separation once.
A legacy follow-up varied the construction seed six times in the same state,
size, target and basis (Table~\ref{tab:clusters}).

\begin{table*}[t]
\caption{\label{tab:clusters}Legacy fixed-cell construction clusters. Values
are point contrasts and out-of-sample force-RMSE ratios; the table does not
repair the paired-UQ or provenance defects described in the text.}
\begin{ruledtabular}
\begin{tabular}{lcccccc}
cluster & 0 & 1 & 2 & 3 & 4 & 5 \\
\colrule
aligned $-$ null (pairs) & 10.88 & 10.69 & 11.01 & 11.28 & 10.44 & 9.73 \\
force match, out of sample & 0.991 & 1.004 & 0.998 & 0.995 & 1.001 & 0.999 \\
\end{tabular}
\end{ruledtabular}
\end{table*}

Their mean is $+10.671$ pairs and the simple cluster-level $t_5$ interval is
$[+10.105,+11.238]$. This is evidence that the large point contrast is not
unique to one construction seed \emph{inside this fixed cell}. It is not a Gate
A replication: only one target, one basis and one $N=108$ state were tested,
with six rather than the planned eight clusters and three rather than four
direct chains.

The legacy uncertainty analysis is also invalid. Aligned and null chains shared
random streams but were described as independent and combined by quadrature;
the between-cluster standard deviation was then divided by a within-cluster
standard error, mixing levels. Direct stationarity checks were disabled, all
clusters inherited one upstream configuration, and the finish-time manifest
records a commit created after the run started. The random field was
prospectively expected to lie between null and aligned but failed that ordering
in two of six clusters; it is now treated as descriptive, not retrospectively
relabelled a successful control. Ratio summaries with near-zero null
denominators are also withdrawn.

A corrected exp11 v3 uses paired seed/start blocks without claiming
proposal-level common-random-number synchrony, independent cluster
equilibration, endpoint and energy stationarity checks, a block-bootstrap
force-match interval, raw per-frame deposition and eight clusters. The earlier
v2 protocol was superseded before production because its aligned-field sign was
not invariant to the SVD convention. V3 has not produced a production result.
The only current conclusion is therefore the
six-seed, fixed-cell point-estimate stability stated above; cross-target,
cross-basis, cross-size and many-body stability remain untested.

%======================================================================
\section{What did not work}
\label{sec:failures}
%======================================================================

\subsection{The \texorpdfstring{$w^{3/2}$}{w3/2} measurement is inconclusive}
\label{sec:w32}

Section~\ref{sec:retraction} predicted, then retracted on analytic grounds,
that at fixed force error the observable damage would grow as $w^{3/2}$. The
measurement does not independently refute 1.5. The Gaussian-shell construction
requires $r_0+3w<r_{\rm on}$; here $r_0=4.2$~\AA{} and
$r_{\rm on}=5.95$~\AA{}, so only $w=0.10$, 0.15, 0.25 and 0.40~\AA{}
are unswitched. On those four points the descriptive slopes are 0.787 for
direct sampling and 0.724 for first order.

No confirmatory interval is available. The four direct estimates share a
trajectory, whereas the historical marginal-error redraw treated them as
independent; the first-order series has no deposited pointwise uncertainty.
The six-point slopes, 0.559 (direct) and 0.452 (first order), additionally mix
width dependence with switching-region truncation at $w=0.65$ and 1.00~\AA{}.
They cannot be used as an exponent test. The analytic retraction still stands
on its stated saturation assumptions; experimentally, the exponent is
unresolved. What survives is the weaker shape-dependence statement of
Sec.~\ref{sec:retraction}.

\subsection{The proposed smoothness-ratio metric is refuted}
\label{sec:smoothness}

We proposed $\sigma_0(\dU)/\mathrm{RMSE}_F$ as an empirical stand-in for the
inverse spectral weighting of Sec.~\ref{sec:frequency}. Against
observable error across the 34-member zoo it gives $\rho = -0.02$ [$-0.37$,
0.35], $n = 34$ noise-subtracted and $\rho = -0.01$ [$-0.37$,
0.37], $n = 34$ raw: on either convention, no
information at all. Proposed and refuted in the same study.

Inside the post hoc band of
Sec.~\ref{sec:regimes} the same quantity gives $\rho = 0.84$ [0.49, 0.98],
$n = 15$ --- not support for the metric: the band is analyst-chosen, its members
dominated by one family of shell perturbations,
and the zoo-level correlation, the pre-specified comparison, is consistent
with zero. The metric is refuted at the level at which it was proposed.

\subsection{An open end-to-end discrepancy}
\label{sec:discrepancy}

In the amplitude sweep of Fig.~\ref{fig:response}(a), at the smallest
perturbation ($\beta\sigma_0(\dU) = 0.427$), three estimates of one shift
disagreed (Table~\ref{tab:discrepancy}): first order
$-3.84 \pm 0.17$ pairs, finite-sample exponential reweighting $-3.884$ at an
effective-sample-size fraction\cite{Kish1965} of 0.83, direct sampling
$-1.95 \pm 0.82$. The two sharing reference samples agree to 1.3\%; the one
needing an independent chain differs from both by a factor of two.

The original six-chain follow-up used the same perturbation but the 5.25~\AA{}
bin rather than the disputed 4.25~\AA{} bin, so it cannot establish reliability
at the endpoint in question. A later exp10 run did measure both bins with eight
reference and eight surrogate HMC chains, bidirectional FEP/MBAR and a
Metropolis cross-check. Its point estimates did not reproduce the old large gap,
which is useful negative evidence, but its own predeclared equivalence result is
underpowered: every primary interval extends outside the $\pm0.5$-pair region.

That run also cannot exclude incomplete relaxation. The code mistakenly called
the two starting-state arms converged when $|\mathrm{gap}|-1.96\,SE\le0.5$;
equivalence requires the \emph{upper} uncertainty bound
$|\mathrm{gap}|+zSE\le0.5$. With the stored final gaps and errors, the correct
upper bounds are about 2.07 and 3.39 pairs. Its MBAR bootstrap concatenated
independent chains, raw per-frame data were not retained, and the finish-time
manifest identifies source committed after the process started. Thus ``the gap
did not reproduce'' is a point-estimate observation, not proof that the old
result was a single-chain fluctuation.

The exp10 v2 pipeline now uses chain-level joint resampling, the correct three-way
equivalence/difference/inconclusive decision, independent presoak starts,
stationarity and overlap gates, raw chain deposition and launch-time source
hashes. No exp10 v2 production result is yet available. The 35\% discrepancy therefore
remains an unresolved end-to-end consistency failure and blocks any claim that
the reference-only predictor is quantitatively validated.

\subsection{The first-order prediction breaks down at large
\texorpdfstring{$\dU$}{dU}, as it must}
\label{sec:breakdown}

Linear response is a first-order truncation and must fail somewhere. A separate
four-model arm, at a budget of 20 configurations in
quick-run mode, illustrates a possible failure but is not production evidence
(Table~\ref{tab:breakdown}): force RMSE reaches $4.34\times10^{-2}~\eVA$
and observable errors 126 pairs, and the aggregate prediction residual is
$\mathbf{30.4\sigma}$, $n = 4$, under legacy error bars. The largest residual is
$60.5\sigma$, for the
$E(3)$-equivariant model, predicted
$-14.2 \pm 7.3$ pairs against a measured $-126.2 \pm 1.8$; the mildest is
\texttt{spline\_k12}, at $1.55\times10^{-3}~\eVA$, predicted $0.03 \pm 0.25$
against $-5.2 \pm 1.9$.

\textbf{Two fields are missing from these records and both weaken what the arm
establishes.} They carry
neither \texttt{second\_order\_ratio} nor the legacy screen field, so the
unvalidated screen of Sec.~\ref{sec:cumulant}(iii) cannot be audited here; and
\textbf{they carry no \texttt{direct\_equilibrated} flag}, so the
stationarity guard of Sec.~\ref{sec:equilibration} was either not run or not
recorded for the arm whose shifts are enormous. That matters more.
In Sec.~\ref{sec:fitted} the same \texttt{egnn\_c8}
architecture, at a \emph{smaller} force error of $1.4\times10^{-2}~\eVA$,
produced an apparent shift of $-95.5$ pairs and was discarded as unequilibrated;
here at $4.34\times10^{-2}~\eVA$ it gives $-126.2$ pairs and is retained. Internal evidence agrees (Table~\ref{tab:breakdown}): a twelvefold
larger observable error at a \emph{smaller} force error than the equilibrated
arm's, in the one arm without an equilibration record, is as well
explained by an unrelaxed chain as by the failure of linear response. Linear
response must break down at large $\dU$, and this arm is consistent with that
without cleanly demonstrating it: the $30.4\sigma$ is an upper
bound on the disagreement attributable to truncation. Re-running with the guard
recorded is the fix; nothing else here depends on it.

\subsection{The warning light does not work}
\label{sec:warninglight}

The amplitude sweep cannot be offered in support of the warning light.
The legacy threshold passes the two smallest amplitudes and rejects the five
larger; but of those two passes only one agrees with direct sampling,
the other being the unresolved discrepancy of
Sec.~\ref{sec:discrepancy}, which the deposited summary records under
the key \texttt{flagged\_cases\_all\_agree: false}.

An exploratory script pooled 89 repository rows carrying both the ratio and a
direct measurement. They are \emph{not} 89 independent validation cases: 64 are
the crossed, common-random-number exp09 cells, and many others share systems,
reference chains and fitted families. Table~\ref{tab:warning} is therefore a
descriptive audit of the historical ratio-$<0.25$ threshold, not an estimate of
population error rates.

\begin{table}[t]
\caption{\label{tab:warning}Historical warning-light row counts. The rows are
clustered and partly reuse direct truth, so the counts have no independent-case
error-rate interpretation.}
\begin{ruledtabular}
\begin{tabular}{lcc}
 & agrees with direct MD & disagrees \\
\colrule
diagnostic says trust & 59 & \textbf{14} \\
diagnostic says beware & 13 & 3 \\
\end{tabular}
\end{ruledtabular}
\end{table}

Fourteen of 73 passing rows disagree by the script's rule, sensitivity is only
$3/17$, and the descriptive AUC is 0.556. Ordinary binomial intervals,
$p$-values and cell-level bootstrap inference are invalid for this clustered
panel and are withdrawn. The threshold was neither calibrated on a development
set nor prospectively tested on held-out field shapes or state points.
\textbf{We withdraw the claim.} The API now names this quantity an
``unvalidated linearity screen''; the ratio is reported descriptively and
nothing in this paper is certified by it.

\subsection{The dilute-gas closed form disagrees}
\label{sec:dilute}

The sharpest end-to-end test of the estimator is the dilute limit of a
pair fluid, where $g(r) = e^{-\beta u(r)} + O(\rho)$ exactly\cite{HansenMcDonald2013},
so a perturbation $\delta u$ shifts the bin counts by
a closed-form quantity. At $\rho^* = 0.079$ and $T^* = 2.510$
on 64 atoms with 6000 reference frames, the sampled covariance estimator and the
exact reweighting identity agree to within 0.003 pairs in every
bin, at most 1.5\% of the shift where it is resolved. Both
disagree with our hand-derived closed form, by $4.7\sigma$ root-mean-square and
$6.5\sigma$ at worst over seven bins. (Our project notes quote $5.4\sigma$; the
deposit gives $4.68\sigma$ and $6.53\sigma$, and we follow the deposit.)

One correction the hand calculation needed, and the estimator did not, is worth
recording. Pair number is conserved, so a perturbation depleting one shell
must enrich others: the pair-separation distribution is normalised. Dropping
that term gives a closed form wrong by a factor of ten in the
outer bins and wrong in sign in the tail. A covariance is mean-subtracted, and
that subtraction \emph{is} the normalisation.

The residual disagreement is most likely the $O(\rho)$ correction to
$g = e^{-\beta u}$, which at $\rho^* = 0.079$ is not negligible. \textbf{This is
not confirmed.} The confirming test --- repeating at a quarter of the density
and checking that the deviation falls --- was not run.

This benchmark also exposed a claim-bearing uncertainty bug. The old
\texttt{reweight()} bootstrap computed the shift as reweighted minus reference
mean but propagated only the reweighted-mean term, so its error could collapse
under concentrated weights while reference-mean uncertainty remained. The code
now resamples the difference jointly and has a regression test. Because legacy
source provenance is invalid, this repair does not rehabilitate any old error
bar; claim-bearing runs require clean regeneration.

%======================================================================
\section{Relation to prior work}
\label{sec:prior}
%======================================================================

\paragraph*{The empirical dissociation.} That force error and simulation quality
come apart is an established empirical finding. Fu \emph{et al.}\cite{Fu2023}
benchmarked machine-learned force fields on simulation tasks and found the
ranking by force error a poor guide to the
ranking by simulation outcome; Stocker \emph{et al.}\cite{Stocker2022} found
graph-network potentials with excellent test errors unreliable in long,
hot molecular dynamics. We do not claim to have reproduced either
benchmark: no published potential and no dynamical metric was evaluated here.
What this paper adds is threefold. \emph{(i) Confound removal.} In those
comparisons models differ simultaneously in force error, architecture, training
data and --- decisively --- the \emph{magnitude} of their error, so shape and
size cannot be separated; here the force norm is fixed by construction to
0.52\% while field direction varies. \emph{(ii) A mechanism.} Those studies
offer an observation; Eq.~\eqref{eq:linear} supplies a candidate mechanism, with
fixed-arm descriptive residuals of $1.01\sigma$ ($n = 8$) and $1.06\sigma$
($n = 10$), not calibrated coverage. \emph{(iii) A
different disease.} Their failure mode is dynamical instability: large $\dU$,
out of distribution, non-perturbative, visible. Ours is in-distribution,
perturbative and silent: nothing blows up, the trajectory is fine, the
diagnostics are healthy, and the answer is wrong by $16\sigma$.

\paragraph*{The identity is not new.} We say so plainly because the
temptation to overclaim is real. Equation~\eqref{eq:linear} is standard
thermodynamic perturbation theory: Zwanzig's free-energy-perturbation
identity\cite{Zwanzig1954} expanded in cumulants, with Bennett's acceptance
ratio\cite{Bennett1976} and its multistate successor\cite{ShirtsChodera2008} as
the machinery around it. The same covariance is a parameter
gradient in differentiable trajectory reweighting, where Thaler and
Zavadlav\cite{Thaler2021} differentiate reweighted ensemble averages with
respect to potential parameters to train against experimental observables, and
Imbalzano \emph{et al.}\cite{Imbalzano2021} use it to propagate committee
uncertainty to thermodynamic averages.
Frederiksen \emph{et al.}\cite{Frederiksen2004} are the deep precedent for the
conceptual claim, showing by Bayesian ensemble sampling that the uncertainty a
potential induces is a property of the quantity predicted, not a number
attached to the model. The clearest candidate increment is the explicit
matched-force-RMSE null/aligned construction and its direct fixed-cell
falsification; its cross-condition generality remains untested.

\paragraph*{Norm-based corrections and the dynamical case.} Wu \emph{et
al.}\cite{Wu2024} derive a correction to lattice thermal conductivity in which
the force error enters through a norm. That is the expected structure
for a transport coefficient: $\delta F$ enters the equations of motion directly,
so the magnitude of the force error is the right variable and no projection
onto an observable is involved. The contrast with the projection mechanism for
static observables is the asymmetry conjectured in
Sec.~\ref{sec:dynamics}, and Ref.~\onlinecite{Wu2024} is independent evidence
for it --- not evidence produced here, since we tested no dynamical
observable. Related work on transport with foundation models\cite{Pota2024}
points the same way.

\paragraph*{Validation practice.} Morrow, Gardner and
Deringer\cite{Morrow2023} argue that machine-learned potentials should be
validated on the properties they will be used to compute rather than on
held-out errors alone. This fixed-cell construction is compatible with that
recommendation and supplies a controlled example of why it can be necessary
rather than merely prudent: the held-out error
is a norm and the property is a projection. Reports
that practitioners already distrust force RMSE\cite{Kovacs2021}, and of
systematic softening in universal potentials with good aggregate
errors\cite{Deng2025}, are consistent with the same reading.

\paragraph*{The practical consequence, and its untested part.} The oracle
diagnostic evaluated here is a \emph{vector} of response scores --- one
$-\beta\,\Cov_0(A_k,\dU)$ per observable of interest --- at one energy
evaluation per stored frame per model. It is not yet a deployable
recommendation: a practitioner has no everywhere-evaluable $U_0$, so $\dU$ is
unavailable.
The usable substitute replaces the unknown truth with a committee mean over
independently trained models\cite{Imbalzano2021}, giving a per-model deviation
from consensus whose spread is an uncalibrated disagreement proxy, not an
observable-error interval. Homogeneous committees can share systematic error,
which cancels in a committee mean and can cause under-reporting; finite panels,
outliers and ensemble mismatch can also overstate disagreement, so no one-sided
guarantee follows. \textbf{This was not tested;} the experiment designed to test
it was not run, and the committee predictor is a hypothesis here, not a finding.

That gap is smaller than it was when this work began, and the reader should know
who has closed part of it. Kellner \emph{et al.}\cite{Kellner2026} build
uncertainty-aware universal potentials trained against several
electronic-structure references and calibrated against experimental
measurements, with a statistical-reweighting protocol that delivers
observable-level uncertainty at close to single-model cost. That is the
practical layer this paper stops short of, arrived at from the opposite
direction: they estimate the uncertainty a real model carries into a real
observable, where we characterise which directions of error a given uncertainty
budget can hide in. The two are complementary, and the honest statement of this
paper's position is that it supplies a mechanism for a problem whose practical
half is already being solved elsewhere.

\paragraph*{Possibility, not prevalence.} One boundary is worth drawing sharply,
because it is where this paper is weakest. Everything demonstrated here works on
error fields we built: they establish that force RMSE \emph{can} be blind to a
large observable error, and say nothing about how often fitting produces error
with appreciable projection onto an observable anyone cares
about. The one arm that could have spoken to prevalence, the fitted zoo of
Sec.~\ref{sec:fitted}, returns a null, for a reason internal to our design: a
two-parameter pair potential is easy enough that every architecture we tried
fits it to $\sim 4\times10^{-4}~\eVA$. The seed pair is the one piece of
evidence the other way, and it is $n = 2$. This paper motivates the experiment
that would settle prevalence --- the same zoo against a many-body reference ---
and does not answer it.

%======================================================================
\section{Scope and limitations}
\label{sec:scope}
%======================================================================

We list the limitations without euphemism, separating those foreclosed by design
from those foreclosed by resources.

\emph{By design.} One reference potential and one state
point, $N = 108$, $\rho^* = 0.790$, $T^* = 1.004$. The analytic
reference is the methodological crux and the
reason no result here is a claim about a chemically realistic system. No
density-functional-theory reference was used and none was needed; no published
machine-learned potential was evaluated, the ten fitted models being deliberately
small surrogates. Stillinger--Weber
silicon\cite{StillingerWeber1985} and an embedded-atom-method
copper\cite{DawBaskes1984} with a second-moment tight-binding embedding
function\cite{FinnisSinclair1984, CleriRosato1993} are implemented and validated
in the same codebase (Appendix~\ref{app:validation}), and \emph{no
observable-error result was produced on either}. One observable class: pair
counts in radial bins, and for the headline demonstration a single bin.
Error fields are pair-radial only --- no angular and no many-body error
structure, precisely where real architectures differ. No dynamical
observable of any kind: no diffusivity, vibrational spectrum,
melting point or elastic constant of any surrogate was measured.

\emph{By resources.} No finite-size study: every number is $N = 108$, and we do
not assert size-independence. The check is
outstanding and not trivial, since pair-count fluctuations in a
closed $NVT$ cell carry a known ensemble dependence\cite{LebowitzPercusVerlet1967}
that Eq.~\eqref{eq:linear} inherits, and the quantity
measured here \emph{is} a pair-count covariance in a closed cell. The seed-pair
observation of Sec.~\ref{sec:fitted} is $n = 2$, an
illustration and not a statistic. The band of Sec.~\ref{sec:regimes} is post hoc:
the window $1.5$--$6.0\times10^{-3}~\eVA$ was chosen by the analyst after
seeing the data and is hard-coded in the figure script, its 15 members
were not pre-registered, six share one force RMSE by
construction, and two are fields we designed. Untested here: the dynamical
conjecture of Sec.~\ref{sec:dynamics}, the committee predictor of
Sec.~\ref{sec:prior}, the remaining candidate explanation for
the unresolved explanations for the discrepancy of
Sec.~\ref{sec:discrepancy}, and the quarter-density
repeat that would confirm the reading of Sec.~\ref{sec:dilute}.

Four further limitations surfaced in preparing this manuscript. \emph{(i)} The
refutation of P1 in Sec.~\ref{sec:regimes} is on designed error
fields, not the architecture-diverse zoo of fitted models the pre-registered
criterion named; the fitted arm cannot decide it. \emph{(ii)} The large-$\dU$
arm of Sec.~\ref{sec:breakdown} lacks not only the second-order diagnostic but
the stationarity flag, so its $30.4\sigma$ cannot be separated from unrelaxed
surrogate chains. \emph{(iii)} Within each arm every surrogate is
measured against one shared reference chain, so the root-mean-square prediction
residuals are not calibration statistics; the legacy eight-chain follow-up is a
crossed, common-random-number design lacking the raw values needed for joint
uncertainty propagation. \emph{(iv)} All claim-bearing legacy runs came from
dirty working trees, and the later exp09--exp11 manifests captured source at
finish rather than launch. Corrected exp09/exp11 v3 and exp10 v2 pipelines
exist, but no production result from them is used in this manuscript.

Finally, two limitations of framing. Nothing here shows
that force error is useless --- Sec.~\ref{sec:regimes} shows the opposite across
a 361-fold range, and the claim is about resolution, not information content ---
and nothing establishes prevalence: the designed fields prove the
pathology can exist, and the fitted arm did not detect it here.

%======================================================================
\section{Conclusion}
\label{sec:conclusion}
%======================================================================

The strongest provisional legacy observation is narrow. In one Lennard--Jones
state, one $N=108$ cell, one pair-radial basis and one target pair count, legacy
artifacts report null and aligned fields whose force RMSE differs by 0.52\% while
their measured target shifts differ by 10.11 pairs. An RMSE-only policy with a
prospectively declared tolerance at least that wide would not resolve them;
this is not a formal statistical-sufficiency or exact-equivalence result. Six
legacy construction seeds reproduce a
similar point contrast inside the same cell, but their random-stream,
stationarity and provenance defects prevent a confirmatory stability claim.

The 34-member designed zoo gives useful descriptive context: force RMSE orders
the panel across a 361-fold range and loses resolution inside one post hoc
factor-2.6 band, while a response score retains a higher rank correlation. The
members are clustered constructions sharing data and random streams, so
ordinary row-bootstrap intervals do not define a model-population inference and
the panel establishes no selector. Real-model benchmarks are qualitatively
compatible with average utility plus individual failures, but do not validate
this study's particular two-regime breakpoint.

The standard response identity remains theoretically useful, but this project
has not yet validated a practical predictor. Its $1.01\sigma$ and
$1.06\sigma$ residual summaries reuse shared reference samples; the first
calibration replication is pseudoreplicated; and the end-to-end exp10 run is
underpowered and failed a correctly evaluated relaxation-equivalence gate. The
second-order ratio also has observed false passes and is now an unvalidated
descriptive screen. Corrected exp09/exp11 v3 and exp10 v2 pipelines freeze launch provenance, preserve
raw chain units and apply joint hierarchical resampling, but have not yet
produced production evidence.

Thus this work is a controlled mechanism demonstration, not a solution to MLIP
selection. The unresolved scientific problem is whether a no-oracle,
observable-specific diagnostic improves prospective selection decisions for
real fitted models under finite reference budgets. Answering it requires the
still-open Gate A (targets, bases, sizes and many-body mechanisms), Gate B (real
training-induced/DFT error fields) and Gate C (blind decision regret and
coverage) experiments.

%======================================================================
\section{Supplementary material}
%======================================================================

The repository currently contains aggregate records for the 34-member designed
zoo, amplitude and width sweeps, the four-model large-$\dU$ arm and the legacy
64-cell exp09 table, together with analysis scripts. As detailed in Data
Availability, these are not yet a complete reproducible deposit: raw chain units
and valid launch provenance are absent for the legacy results.

\begin{acknowledgments}
[FUNDING STATEMENT TO BE COMPLETED]
\end{acknowledgments}

\section*{AUTHOR DECLARATIONS}

\subsection*{Conflict of Interest}
The authors have no conflicts to disclose.

\subsection*{Author Contributions}
[AUTHOR CONTRIBUTIONS TO BE COMPLETED]

\subsection*{Ethics Approval}
Ethics approval was not required for this work, which involves no human or
animal subjects.

\section*{DATA AVAILABILITY}

\textbf{[PLACEHOLDER --- no deposit has been made; the DOI and reference number
below are to be filled in on deposit, and this paragraph is not a statement of
fact until they are.]} The data that support the findings of this study will be
openly available in Zenodo at http://doi.org/[DOI], reference number [DEPOSIT
ID].\cite{Deposit} The
current repository contains source, configurations and aggregate legacy outputs
such as \texttt{summary.json} and \texttt{records.json}; it does \emph{not}
contain the raw chain arrays needed to recompute every legacy uncertainty. The
old manifests were written after result generation and all claim-bearing legacy
runs were dirty, so their recorded commit/digest cannot prove which loaded source
produced them. The corrected harness writes launch and finish snapshots, detects
source changes, hashes artifacts and stores per-chain arrays for future corrected runs,
but no such production artifact is yet part of the evidence. The legacy campaign
must therefore be rerun from a frozen revision before any final deposit may claim
byte-for-byte reproducibility. No external dataset was used, downloaded, or
required: every configuration analysed here was generated by the deposited code
from the stated seeds. All analysis scripts that produce the figures and tables
in this article are included in the same deposit.

%======================================================================
\appendix
%======================================================================

\section{Simulation and estimator validation}
\label{app:validation}

Table~\ref{tab:cutoff} gives the energy and force discontinuities of the four
cutoff modes, measured at $r_c \mp 10^{-7}$~\AA. Only \texttt{shifted\_force}
and \texttt{switched} remove the force discontinuity; \texttt{shifted}, the most
common choice, leaves a force jump identical to that of the plain truncation.
Since every result in this paper is a small systematic difference between two
ensembles, an impulse delivered whenever a pair crosses the cutoff would be
indistinguishable from the effect under study, and \texttt{shifted\_force} is
used throughout.

Table~\ref{tab:validation} lists the checks run against targets from outside the
codebase. Two entries deserve comment. The Stillinger--Weber silicon cohesive
energy is an \emph{exactness check}: at the diamond lattice constant every bond
sits at the two-body minimum, every angle is exactly tetrahedral so the
three-body term vanishes, and the second-neighbour shell lies outside the
cutoff, so the answer is $-2\epsilon$ identically and the code returns
$-4.3366000$~eV/atom against $-4.33660$~eV/atom. And the embedded-atom copper
reproduces its three fitted targets --- lattice constant, cohesive energy, bulk
modulus --- exactly, while giving a maximum phonon frequency of 5.14~THz against
roughly 7.5~THz for real copper, about 30\% soft at the zone boundary. That is
not a defect, since the reference potentials define this study's ground truth
and are not claims about experiment. It is the thesis of this paper arriving
from another direction: fitting a quantity exactly, even a second-derivative
quantity such as the bulk modulus, does not constrain the quantities that were
not fitted.

\section{Statistical estimators and their validation}
\label{app:statistics}

Means use Flyvbjerg--Petersen blocking\cite{Flyvbjerg1989}: the sample series is
repeatedly halved by pairwise averaging, the apparent standard error is computed
at each level, and the plateau is read off over levels retaining at least eight
blocks. Statistics that are not means --- covariances, rank correlations, fitted
slopes --- use a moving-block bootstrap\cite{Kunsch1989, PolitisRomano1994} with
block length four integrated autocorrelation times of the product series
$(A_m - \bar A)(\dU_m - \overline{\dU})$, resampled with replacement; percentile
intervals are quoted. The product series rather than the observable's own is
used because the observable's correlation time underestimates the block length
whenever $\dU$ varies slowly, which is precisely the systematic-error case this
paper is about. A delete-one jackknife with a different bias structure is
available as a cross-check.

The estimators were validated on an AR(1) process of known correlation time: the
blocking estimate recovers the naive standard error inflated by
$\sqrt{2\tau_{\rm int}}$ to within 30\%, and the measured integrated
autocorrelation time matches the analytic $1/2 + \phi/(1-\phi)$ to within 15\%.
The response estimators were validated against closed forms on a harmonic
reference perturbed by a linear field, where the cumulant expansion terminates
and each order can be isolated: $\avg{x}$ shifts by exactly $-\varepsilon/k$
(pure first order), $\avg{x^2}$ by exactly $\varepsilon^2/k^2$ (pure second
order), and the Zwanzig free-energy shift is $-\varepsilon^2/2k$; all three are
recovered within statistical error. The reweighting estimator additionally
reports the Kish effective sample size\cite{Kish1965} and the largest
single-frame weight fraction, since an exponential average can be
precise-looking and meaningless.

%======================================================================
% FIGURES
%======================================================================

\begin{figure*}[t]
\includegraphics[width=6.6in]{headline_mechanism.pdf}
\caption{\label{fig:mechanism}%
Two descriptive legacy panels at nearly fixed force RMSE.
(a) Target-bin shifts against $\rho(A,\dU)$ for four constructed fields at each
of two force levels; vertical bars are marginal blocking errors, not a joint
contrast uncertainty, because fields share reference/direct streams.
(b) Direct and first-order point estimates over a six-width fixed-force sweep.
Only $w=0.10$, 0.15, 0.25 and 0.40~\AA{} satisfy
$r_0+3w<r_{\rm on}$; their descriptive slopes are 0.787 and 0.724. Open points
and shading mark $w=0.65$ and 1.00~\AA{}, whose Gaussian tails enter the cutoff
switch. No joint curve-norm uncertainty was deposited, so no exponent interval
or confirmatory comparison is shown. Neither panel supplies independent
replication or a model-selection result.}
\end{figure*}

\begin{figure}[t]
\includegraphics[width=3.3in]{headline_prediction.pdf}
\caption{\label{fig:prediction}%
Measured against predicted shift in the target pair count, in pairs, for all
eight designed error fields ($n = 8$; four fields at each of two force levels).
The prediction is $-\beta\,\Cov_0(A,\dU)$ evaluated on reference-ensemble
samples alone --- no molecular dynamics or Monte Carlo with any surrogate
entered it --- and the measurement comes from explicit Hamiltonian Monte Carlo
sampling of each perturbed potential. The line is the identity. Horizontal and
vertical bars are the deposited marginal prediction and measurement errors.
The legacy root-mean-square residual is $1.01\sigma$, but that is not a
calibration statistic: all rows share one reference mean, are clustered by
construction and lack joint covariance.
The legacy exp09 follow-up is also not a calibration: its 64 crossed cells reuse
the same direct means and common random streams, omit joint prediction
uncertainty and lack deposited raw chain values (Sec.~\ref{sec:calibration}).
The plotted panel is retained as a fixed-arm tracking description, not an
independent calibration test.}
\end{figure}

\begin{figure*}[t]
\includegraphics[width=6.6in]{exp07_designed_counterexamples_counterexamples.pdf}
\caption{\label{fig:counterexamples}%
Legacy difference curves for scalar-null, aligned and both random fields, one
panel per force level. Curve points use the separate eight-bin grid; shading
marks the actual 3.4--3.9~\AA{} target and diamonds plot its independently
deposited measurement. Solid/dashed coloured lines are measured curves and
dotted lines are first-order curves; plotted bars are marginal blocking errors.
All fields share streams and the launch source is invalid, so the figure is not
a confirmatory replication. Selecting the largest of eight curve bins biases a
maximum upward; the earlier $11.36\pm0.95$ maximum-based inference is withdrawn.}
\end{figure*}

\begin{figure*}[t]
\includegraphics[width=6.6in]{headline_regimes.pdf}
\caption{\label{fig:regimes}%
Fixed, clustered legacy zoo of 34 designed surrogates. (a) Raw eight-bin
observable-error norm against force RMSE; the analyst-chosen post-hoc window is
shaded, deposited Monte Carlo noise scales are drawn, and six noise-dominated
rows are crossed. (b) The 15 window members ordered by force RMSE, with the same
noise-scale whiskers. Their raw range is $12.1\times$ ($5.9\times$ after
removing the designed extremes); the withdrawn floor-sensitive convention gave
$30\times$. The displayed rank ranges are row-resampling sensitivities for this
fixed panel: force $\rho=0.83$ [0.67, 0.93] overall and 0.34 [$-0.27$, 0.78]
inside the window; response $\rho=0.94$ [0.70, 1.00] inside. Shared trajectories,
family clustering and post-hoc selection preclude population or
selection-utility inference.}
\end{figure*}

\begin{figure*}[t]
\includegraphics[width=6.6in]{exp05_proxy_correlation_proxy_correlation.pdf}
\caption{\label{fig:proxies}%
Proxy metrics against measured observable error across the 34 designed
surrogates. \textbf{This figure is drawn on the raw (non-noise-subtracted)
observable-error series, while Fig.~\ref{fig:regimes} and the running text use
the noise-subtracted one}; the printed panel titles therefore quote the
raw-convention values, $\rho = 0.83$ [0.68, 0.92] in (a) and $-0.01$
[$-0.37$, 0.37] and 0.92 [0.81, 0.97] in (c), all $n = 34$, against the
noise-subtracted
values quoted below and in the text. Both conventions appear in the caption of
Table~\ref{tab:regimes} and differ by less than 0.01 in every $\rho$.
(a) Observable error against force RMSE, with standard-error bars;
the noise-subtracted rank correlation for this panel is given in
Fig.~\ref{fig:regimes}(a). (b) Measured against predicted observable error, the
prediction being the norm of $-\beta\,\Cov_0(A_k,\dU)$ from reference-ensemble
samples alone. (c) Spearman $\rho$ with percentile row-resampling sensitivity ranges for every proxy
metric. Panel (c) is where two results of Sec.~\ref{sec:regimes} are visible:
the smoothness ratio $\sigma_0(\dU)/\mathrm{RMSE}_F$, proposed in this work,
gives $\rho = -0.02$ [$-0.37$, 0.35], $n = 34$ and is refuted; the per-atom
energy spread gives $\rho = 0.92$ [0.82, 0.97], $n = 34$ and outranks every
force metric, which the theory does not predict.}
\end{figure*}

\begin{figure*}[t]
\includegraphics[width=6.6in]{exp06_response_validation_response_validation.pdf}
\caption{\label{fig:response}%
Legacy response-estimator and width-sweep diagnostics.
(a) First-order, exactly reweighted and directly sampled shifts in the peak bin,
in pairs, against the amplitude of a Gaussian shell perturbation at
$r_0 = 4.2$~\AA, $w = 0.35$~\AA. All three agree at amplitude
$1\times10^{-3}$ ($\beta\sigma_0(\dU) = 0.854$, the largest value at which they
do); above it the first-order estimate grows linearly to $-245.5 \pm 10.9$ pairs
at the largest amplitude, the direct measurement saturates at
$-144.1 \pm 0.6$, and the reweighting estimate collapses to a constant $-43.9$
as its effective-sample-size fraction falls from 0.83 to
$5\times10^{-4}$\cite{Bennett1976}. The
disagreement at the \emph{smallest} amplitude is the unresolved discrepancy of
Sec.~\ref{sec:discrepancy}.
(b) Norm of the measured shift at fixed force RMSE against width $w$, on
log--log axes. Only $w=0.10$, 0.15, 0.25 and 0.40~\AA{} satisfy the construction's
$r_0+3w<r_{\rm on}$ unswitched condition; their descriptive slopes are 0.787
(direct) and 0.724 (first order). The two larger widths enter the switching
region, so the old six-point slopes cannot test the proposed exponent. No valid
interval is quoted: the direct points share a trajectory and the first-order
series has no deposited pointwise uncertainty. The experiment is inconclusive
about 1.5; Sec.~\ref{sec:w32} gives the bounded interpretation.}
\end{figure*}

%======================================================================
% TABLES
%======================================================================

% Source: results/exp07_designed_counterexamples/records.json
\begin{table*}[t]
\caption{\label{tab:counterexamples}%
Four designed error fields at two matched force-RMSE levels. $\rho(A,\dU)$ is
the Pearson correlation between the target observable (the pair count between
3.4 and 3.9~\AA) and the error field under the reference ensemble; the predicted
shift is $-\beta\,\Cov_0(A,\dU)$ from reference-ensemble samples alone; the
measured shift is from explicit Hamiltonian Monte Carlo sampling of the
perturbed potential. Force RMSE is quoted out of sample, on the half of the
reference trajectory not used to construct the fields; within each level the
four values agree to \textbf{0.52\%}. Each $\pm$ in this table is a deposited
marginal standard error from blocking for means or a moving-block bootstrap for
covariances; it is not a joint contrast interval. The $\sigma$-distance is
$|\text{measured}|/|\text{its standard error}|$ (in Table~\ref{tab:breakdown}
the same column heading means the prediction residual instead). Values are
rounded to two significant figures in the uncertainty; the deposited records
hold full precision. The ``null'' field here is the \emph{scalar-null}
construction of Sec.~\ref{sec:designed}, orthogonal to this one target bin; the
\emph{curve-null} field of Sec.~\ref{sec:regimes} is a different object, built
against all eight bins of the difference curve.}
\begin{ruledtabular}
\begin{tabular}{llccr@{\,$\pm$\,}lr@{\,$\pm$\,}lc}
Level & Field & RMSE$_F$ (out of sample, $\eVA$) & $\rho(A,\dU)$ &
\multicolumn{2}{c}{predicted (pairs)} & \multicolumn{2}{c}{measured (pairs)} &
$\sigma$ \\
\colrule
$1.0\times10^{-3}$ & scalar-null     & $1.00054\times10^{-3}$ & $+0.009$ & $-0.028$ & $0.083$ & $-0.55$ & $0.58$ & 0.9 \\
                   & random (seed 1) & $0.99896\times10^{-3}$ & $+0.110$ & $-0.327$ & $0.083$ & $-1.45$ & $0.58$ & 2.5 \\
                   & random (seed 0) & $0.99775\times10^{-3}$ & $-0.430$ & $+1.44$ & $0.10$ & $+0.84$ & $0.59$ & 1.4 \\
                   & aligned         & $0.99535\times10^{-3}$ & $-0.582$ & $+2.55$ & $0.14$ & $+1.83$ & $0.58$ & 3.2 \\
\colrule
$4.0\times10^{-3}$ & scalar-null     & $4.00217\times10^{-3}$ & $+0.009$ & $-0.11$ & $0.33$ & $-0.22$ & $0.64$ & 0.3 \\
                   & random (seed 1) & $3.99586\times10^{-3}$ & $+0.110$ & $-1.31$ & $0.33$ & $-1.48$ & $0.58$ & 2.6 \\
                   & random (seed 0) & $3.99100\times10^{-3}$ & $-0.430$ & $+5.78$ & $0.42$ & $+5.32$ & $0.59$ & 9.0 \\
                   & aligned         & $3.98141\times10^{-3}$ & $-0.582$ & $+10.21$ & $0.56$ & $+9.89$ & $0.61$ & 16.2 \\
\end{tabular}
\end{ruledtabular}
\end{table*}

% Source: results/exp07_designed_counterexamples/summary.json (null_space_generalisation)
%         and results/exp07_designed_counterexamples/generalisation.json
\begin{table}[t]
\caption{\label{tab:budget}%
How many frames the null-space construction needs. The field is built on
$n_{\rm construct}$ frames of the reference trajectory and evaluated on disjoint
frames. In sample the predicted shift is zero to machine precision by
construction; out of sample it is not, and the table shows that it remains small
from 250 frames upward. For scale, the aligned field at the same force error
($4\times10^{-3}~\eVA$) has a predicted shift of $10.21$ pairs, so the
legacy point-ratio suppression out of sample on this chain is $360\times$ at
2000 construction frames and $72\times$ at the least favourable row. These
ratios share one evaluation trajectory, have no construction-cluster interval,
and are unstable when the near-zero null denominator moves; they are descriptive,
not release effect sizes. An earlier construction using 30 frames
failed to generalise, but that run predates the deposited records and its
artefact was not retained, so no number for it is quoted. $\pm$ is a standard
error.}
\begin{ruledtabular}
\begin{tabular}{crr@{\,$\pm$\,}l}
$n_{\rm construct}$ & in sample (pairs) & \multicolumn{2}{c}{out of sample (pairs)} \\
\colrule
250  & $9.1\times10^{-15}$ & $0.041$ & $0.067$ \\
500  & $1.2\times10^{-15}$ & $0.142$ & $0.071$ \\
1000 & $5.0\times10^{-15}$ & $0.037$ & $0.077$ \\
2000 & $8.4\times10^{-15}$ & $0.028$ & $0.078$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

% Source: results/exp05_proxy_correlation/{summary,records}.json and
%         scripts/compute_band_correlations.py (seed 20240517, 2000 resamples)
\begin{table}[t]
\caption{\label{tab:regimes}%
Two regimes, across the 34-member designed-surrogate zoo and within the band.
All correlations are Spearman rank correlations against the measured observable
error, computed on the noise-subtracted series (Sec.~\ref{sec:uncertainty}),
with percentile row-resampling intervals over zoo members from 2000 resamples,
seed 20240517. Because members are clustered parameter families sharing a
reference trajectory and random streams, these intervals are descriptive
sensitivity summaries and have no model-population coverage interpretation. The
two outermost bins sit at the $7.0$~\AA{} cutoff and,
averaged over the 34 members, carry 28\% of the squared norm of the measured
difference curve against 29\% for the single bin at 4.25~\AA, so this observable
error is not a cutoff artefact. \textbf{The band --- force RMSE in
$1.5$--$6.0\times10^{-3}~\eVA$ --- is analyst-chosen and post hoc}, fixed after
inspecting the data and not pre-registered. The response prediction is the norm
of $-\beta\,\Cov_0(A_k,\dU)$. Footnote: on the raw (non-noise-subtracted) series
the zoo values are force RMSE $\rho = 0.83$ [0.68, 0.92], force MAE
$\rho = 0.85$ [0.70, 0.94], per-atom energy spread $\rho = 0.92$ [0.81, 0.97],
smoothness ratio $\rho = -0.01$ [$-0.37$, 0.37], response prediction
$\rho = 0.98$ [0.95, 0.99], all $n = 34$ with 2000 resamples; every value moves
by less than 0.01. Band robustness (\texttt{compute\_revision\_statistics.py}):
removing the two designed fields leaves $n = 13$ with $\rho$(force RMSE)
$= 0.33$ [$-0.42$, 0.84] and $\rho$(response prediction) $= 0.91$ [0.64, 1.00];
the nine shell-family members, whose force RMSE values are all distinct, give
0.28 [$-0.63$, 1.00] and 0.98 [0.86, 1.00], $n = 9$. These two intervals use the
same 2000-resample percentile bootstrap as the rest of the table, on an
independent generator stream, and are wide at $n = 13$ and $n = 9$; the
force-RMSE interval includes zero in both subsets and the response-prediction
interval in neither. Withheld top-$k$ statistics: on the raw series
\texttt{summary.json} gives top-three overlaps from 0.00 to 0.67 and top-five
from 0.40 to 0.80 across the nine proxy metrics; no noise-subtracted top-$k$
series was deposited, so their dependence on the convention cannot be checked.}
\begin{ruledtabular}
\begin{tabular}{lcc}
 & across the zoo ($n = 34$) & within the band ($n = 15$) \\
\colrule
force-RMSE spread & $361\times$ & $2.59\times$ \\
observable-error spread & --- & $12\times$ raw; $30\times$ floor-sensitive \\
$\rho$(force RMSE) & 0.83 [0.67, 0.93] & 0.34 [$-0.26$, 0.77] \\
$\rho$(response prediction) & 0.98 [0.95, 0.99] & 0.94 [0.76, 1.00] \\
$\rho$(smoothness ratio) & $-0.02$ [$-0.37$, 0.35] & 0.84 [0.49, 0.98] \\
$\rho$(per-atom energy spread) & 0.92 [0.82, 0.97] & 0.91 [0.71, 0.99] \\
\end{tabular}
\end{ruledtabular}
\end{table}

% Source: results/exp03_model_zoo/records.json, summary.json
\begin{table*}[t]
\caption{\label{tab:zoo}%
The ten fitted models, all trained on 400 configurations of Lennard-Jones argon
with training and test data drawn from separate Markov chains. The predicted
shift is $-\beta\,\Cov_0(A,\dU)$ from reference-ensemble samples alone; the
measured shift is from direct sampling of each fitted model. The
root-mean-square prediction residual over the ten is $1.06\sigma$. \textbf{The
rank correlations from this arm are not reportable}: every interval spans zero
(force RMSE $\rho = 0.43$ [$-0.35$, 0.96], $n = 10$), because at this data
budget on this reference nearly every architecture is good enough that its
observable error is indistinguishable from zero. Nothing about ranking is
concluded from this arm. The two \texttt{egnn\_c8} rows differ only in
initialisation seed and are an illustration at \textbf{$n = 2$}, not a
statistic. The second-order ratio's legacy threshold passes 8 of 10 models and
rejects the EGNN pair, while the largest prediction residual belongs to
\texttt{linear\_l2}, which passes. This is reported as an audit of an
unvalidated screen, not a trust flag. The
$\sigma$ column is $|\text{measured}|/|\text{its standard error}|$, as in
Table~\ref{tab:counterexamples}. Residual decomposition for this arm
(\texttt{compute\_revision\_statistics.py}): seven of ten residuals negative,
mean $-0.49\sigma$, scatter $1.00\sigma$. A sibling arm at 40 training
configurations, reported in Sec.~\ref{sec:fitted}, gives $1.00\sigma$ over the
eight models that passed the stationarity check; the two that failed it, both
\texttt{egnn\_c8} seeds, showed apparent shifts of $-95.5 \pm 1.7$ and
$-112.8 \pm 2.8$ pairs and were excluded. $\pm$ is a standard error.}
\begin{ruledtabular}
\begin{tabular}{llcr@{\,$\pm$\,}lr@{\,$\pm$\,}lccc}
Model & Architecture & RMSE$_F$ ($\eVA$) &
\multicolumn{2}{c}{predicted (pairs)} & \multicolumn{2}{c}{measured (pairs)} &
$\sigma$ & 2nd-order ratio & legacy screen pass \\
\colrule
\texttt{spline\_k8}    & pair spline, 8 knots   & 0.00472 & $+1.67$ & $0.11$ & $+0.98$ & $0.56$ & 1.7 & 0.017 & yes \\
\texttt{spline\_k24}   & pair spline, 24 knots  & 0.00088 & $+0.245$ & $0.067$ & $+0.05$ & $0.60$ & 0.1 & 0.090 & yes \\
\texttt{linear\_l2}    & linear ACE-style, $l\le2$ & 0.00070 & $-0.044$ & $0.035$ & $-0.92$ & $0.55$ & 1.7 & 0.131 & yes\footnotemark[1] \\
\texttt{linear\_l4}    & linear ACE-style, $l\le4$ & 0.00065 & $-0.064$ & $0.022$ & $-0.48$ & $0.61$ & 0.8 & 0.005 & yes \\
\texttt{bpnn\_16x2\_s0} & Behler--Parrinello, $16\times2$\footnotemark[2] & 0.00042 & $+0.250$ & $0.043$ & $-0.07$ & $0.63$ & 0.1 & 0.009 & yes \\
\texttt{bpnn\_16x2\_s1} & Behler--Parrinello, $16\times2$\footnotemark[2] & 0.00041 & $+0.257$ & $0.042$ & $-0.47$ & $0.58$ & 0.8 & 0.010 & yes \\
\texttt{bpnn\_64x2\_s0} & Behler--Parrinello, $64\times2$\footnotemark[2] & 0.00044 & $+0.236$ & $0.041$ & $+0.59$ & $0.62$ & 1.0 & 0.014 & yes \\
\texttt{bpnn\_64x2\_s1} & Behler--Parrinello, $64\times2$\footnotemark[2] & 0.00040 & $+0.235$ & $0.041$ & $+0.37$ & $0.56$ & 0.6 & 0.018 & yes \\
\texttt{egnn\_c8\_s0}  & $E(3)$-equivariant, seed 0 & 0.00450 & $-0.98$ & $0.21$ & $-0.15$ & $0.57$ & 0.3 & 0.412 & no \\
\texttt{egnn\_c8\_s1}  & $E(3)$-equivariant, seed 1 & 0.00609 & $-3.03$ & $0.29$ & $-4.10$ & $0.69$ & 6.0 & 0.142 & no \\
\end{tabular}
\footnotetext[1]{Largest prediction residual in this arm ($-1.60\sigma$), yet
passed the legacy screen, one of the false-pass examples motivating its
withdrawal.}
\footnotetext[2]{These four records carry \texttt{fit\_converged = false}: the
optimiser reached its iteration limit rather than its tolerance. They are
reported because their force errors are the smallest in the zoo and dropping
them would be a post hoc filter.}
\end{ruledtabular}
\end{table*}

% Source: results/exp03_model_zoo_n400/records.json, summary.json
%         (directory name is historical; the arm is at training budget 20)
\begin{table}[t]
\caption{\label{tab:breakdown}%
The large-$\dU$ arm: four models fitted at a training budget of 20
configurations and run in the reduced quick-run mode, which puts them well
outside the perturbative regime. (The deposited directory is named
\texttt{exp03\_model\_zoo\_n400} for historical reasons; the records carry
\texttt{budget: 20}.) The aggregate root-mean-square prediction
residual is $30.4\sigma$, $n = 4$, against $1.01\sigma$ and $1.06\sigma$ in the
perturbative arms. \textbf{These records do not carry the
\texttt{second\_order\_ratio} or legacy screen fields, so the
unvalidated second-order screen cannot be audited against this arm, \emph{and they
carry no \texttt{direct\_equilibrated} flag}, so the stationarity guard of
Sec.~\ref{sec:equilibration} was not recorded here.} Compare the equilibrated
400-configuration arm of Table~\ref{tab:zoo}: \texttt{linear\_l4} at
$6.48\times10^{-4}~\eVA$ shows $-0.48 \pm 0.61$ pairs there, against
\texttt{linear\_l3}'s $-6.1 \pm 2.4$ at the \emph{smaller}
$5.08\times10^{-4}~\eVA$ here. The $\sigma$ column in this table is the
prediction residual $|$measured $-$ predicted$|$/error, not $|$measured$|$/error
as in Tables~\ref{tab:counterexamples} and \ref{tab:zoo}. $\pm$ is a standard
error.}
\begin{ruledtabular}
\begin{tabular}{lcr@{\,$\pm$\,}lr@{\,$\pm$\,}lc}
Model & RMSE$_F$ ($\eVA$) & \multicolumn{2}{c}{predicted (pairs)} &
\multicolumn{2}{c}{measured (pairs)} & $\sigma$ \\
\colrule
\texttt{spline\_k12}    & $1.55\times10^{-3}$ & $+0.03$ & $0.25$ & $-5.2$ & $1.9$ & 2.8 \\
\texttt{linear\_l3}     & $5.08\times10^{-4}$ & $+0.180$ & $0.092$ & $-6.1$ & $2.4$ & 2.6 \\
\texttt{bpnn\_16x2\_s0} & $1.53\times10^{-2}$ & $-4.9$ & $2.3$ & $-14.5$ & $1.8$ & 5.3 \\
\texttt{egnn\_c8\_s0}   & $4.34\times10^{-2}$ & $-14.2$ & $7.3$ & $-126.2$ & $1.8$ & 60.5 \\
\colrule
\multicolumn{6}{l}{aggregate rms residual} & \textbf{30.4} \\
\end{tabular}
\end{ruledtabular}
\end{table}

% Source: results/exp06_response_validation/amplitude_records.json (rows 1-3)
%         and the recorded output of scripts/check_between_chain_scatter.py
\begin{table}[t]
\caption{\label{tab:discrepancy}%
The open discrepancy of Sec.~\ref{sec:discrepancy}. Upper block: three estimates
of the same shift at the smallest perturbation amplitude of the sweep, in the
peak bin at 4.25~\AA. The two estimators sharing the reference samples agree to
1.3\%; the direct estimate, which needs an independent chain, differs from both
by a factor of two. Lower block: the between-chain diagnostic, run on the same
perturbation but on the bin at 5.25~\AA. Because it is a different endpoint, it
does not exclude incomplete relaxation or shared-reference effects at 4.25~\AA.
\textbf{The legacy comparison remains underpowered and non-equivalent; no listed
explanation is excluded by this table.} The lower block is the recorded output of
the deposited script \texttt{check\_between\_chain\_scatter.py} and is not
written to a \texttt{results/*.json} file. $\pm$ is a standard error.}
\begin{ruledtabular}
\begin{tabular}{lr}
Quantity & Value (pairs) \\
\colrule
\multicolumn{2}{l}{\emph{Peak bin, 4.25~\AA, amplitude $5\times10^{-4}$}} \\
first order & $-3.84 \pm 0.17$ \\
exact reweighting (ESS fraction 0.83) & $-3.884$ \\
direct sampling & $-1.95 \pm 0.82$ \\
\colrule
\multicolumn{2}{l}{\emph{Between-chain diagnostic, bin at 5.25~\AA, $n = 6$ seed pairs}} \\
within-chain blocking error & 0.589 \\
between-chain scatter & 0.312 \\
ratio (between/within) & 0.53 \\
six-seed mean shift & $+2.377 \pm 0.144$ \\
fresh-chain first order & $+1.725 \pm 0.101$ \\
fresh-chain exact reweighting & $+1.757$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

% Source: docs/methods.md Sec. 1 / docs/validation.md Sec. 2
\begin{table}[t]
\caption{\label{tab:cutoff}%
Energy and force discontinuities of the four Lennard-Jones cutoff modes,
measured at $r_c \mp 10^{-7}$~\AA{} with $r_c = 7.0$~\AA. The
\texttt{shifted} mode, which is the common choice, removes the energy jump and
leaves the force jump untouched. \texttt{shifted\_force} is used throughout this
work. These values were measured outside the reported experiment campaign and
are recorded in \texttt{docs/methods.md} in the deposit rather than in a
\texttt{results/*.json} file.}
\begin{ruledtabular}
\begin{tabular}{lcc}
Mode & $|\Delta u|$ (eV) & $|\Delta F|$ ($\eVA$) \\
\colrule
\texttt{truncated}     & $2.4\times10^{-4}$  & $1.8\times10^{-4}$ \\
\texttt{shifted}       & $1.8\times10^{-11}$ & $1.8\times10^{-4}$ \\
\texttt{shifted\_force}& $8.7\times10^{-19}$ & $1.6\times10^{-11}$ \\
\texttt{switched}      & $2.2\times10^{-19}$ & $1.4\times10^{-16}$ \\
\end{tabular}
\end{ruledtabular}
\end{table}

% Source: docs/validation.md
\begin{table}[t]
\caption{\label{tab:validation}%
Validation against targets from outside the codebase. The Stillinger--Weber
entry is an \emph{exactness check} and carries its full digits for that reason:
at the diamond lattice constant the answer is $-2\epsilon$ identically. The
embedded-atom copper entry is the one worth dwelling on --- the potential
reproduces its three fitted targets exactly and still gives a maximum phonon
frequency about 30\% below copper's, which is the statement this paper makes
about force error arriving from a different direction. These checks were run
outside the reported experiment campaign; their values are recorded in
\texttt{docs/validation.md} in the deposit rather than in a
\texttt{results/*.json} file, except the Steinhardt row, which is reproduced by
the deposited test suite (\texttt{tests/test\_structure\_observables.py}).
$\pm$ is a standard error.}
\begin{ruledtabular}
\begin{tabular}{lll}
Check & Target & Obtained \\
\colrule
SW Si cohesive energy & exactly $-2\epsilon = -4.33660$~eV/atom & $-4.3366000$ \\
LJ fcc lattice sum & $-8.6108\,\epsilon$/atom & $-8.603$ (cutoff-extrapolated) \\
EAM Cu lattice constant & 3.615~\AA & 3.6150~\AA \\
EAM Cu cohesive energy & $-3.54$~eV/atom & $-3.5400$~eV/atom \\
EAM Cu bulk modulus & 140~GPa & 140.0~GPa \\
EAM Cu max.\ phonon freq. & $\approx 7.5$~THz (real Cu) & 5.14~THz \\
fcc/bcc/sc $Q_6$ & 0.5745 / 0.5106 / 0.3536 & 0.5745 / 0.5107 / 0.3536 \\
SOAP rotational invariance & 0 & $8\times10^{-15}$ \\
ACSF rotational invariance & 0 & $2\times10^{-15}$ \\
HMC $\avg{U}$ (Einstein crystal) & 0.41363~eV & $0.4146 \pm 0.0020$ ($0.5\sigma$) \\
HMC $\Var(U)$ (Einstein crystal) & 0.003564~eV$^2$ & 0.003459~eV$^2$ \\
Phonon dispersion & $2\sqrt{k/m}\,|\sin(qa/2)|$ & $10^{-5}$ relative \\
\end{tabular}
\end{ruledtabular}
\end{table}

\bibliographystyle{aipnum4-2}
\bibliography{refs}

\end{document}
